# خطة التنفيذ — إصلاحات الترحيل وتجربة الإدخال
**التاريخ:** 2026-07-30 · **الحالة:** مُعتمدة، قيد التنفيذ

> كل بند موثّق بالسطر والملف بعد فحص فعلي للكود، لا افتراضات.
> الترتيب مقصود: البنية التحتية أولاً لأن باقي البنود تعتمد عليها.

---

## المرحلة 0 — الترحيل الصحيح طويل الأمد (أساس كل ما بعده)

### 0.1 المشكلة الجذرية المؤكدة
`schemaVersion = 3` ثابت منذ الإصدار الأول، بينما أُضيفت أعمدة كثيرة
بعده. `onUpgrade` ينشئ جداول فقط ولا يحوي **أي** `addColumn` في المشروع.

النتيجة: قواعد البيانات القديمة تفتقر لأعمدة يولّدها Drift في `INSERT`
=> `SQLite error 1`. ظهر في الوكالات، وكان ينتظر في `work_orders`.

الحل الحالي (`ensurePoaColumns` / `ensureUpgradeTableColumns`) شبكة أمان
تعمل في `beforeOpen`، لكنها **ليست** الآلية الصحيحة.

### 0.2 التنفيذ
| # | الإجراء |
|---|---|
| 0.2.1 | رفع `schemaVersion` من 3 إلى **4** |
| 0.2.2 | كتابة `from < 4` في `onUpgrade` يضم كل `addColumn` الناقصة صراحةً عبر `m.addColumn(table, table.column)` |
| 0.2.3 | إبقاء `ensure*Columns` كشبكة أمان (idempotent) للقواعد التي لا تمر بالترحيل |
| 0.2.4 | إضافة اختبار `schema_migration_test.dart`: إنشاء قاعدة v3 → ترقية → التحقق من وجود كل عمود |
| 0.2.5 | توثيق القاعدة في `CONSTITUTION.md`: **ممنوع** إضافة عمود دون رفع `schemaVersion` + `addColumn` |

### 0.3 معيار القبول
- إنشاء قاعدة بمخطط v3 ثم فتحها بالإصدار الجديد ← كل الأعمدة موجودة
- `flutter test` يمر
- إضافة وكالة / أمر عمل / مرجع مكتبة تنجح على قاعدة قديمة

---

## المرحلة 1 — البحث داخل كل الحقول المرجعية

### 1.1 المطلوب
كل حقل يعرض بيانات أدخلها المستخدم سابقاً (الموكل، الوكالة، الخصم،
اسم الدعوى، رقم الدعوى، الشريك، المدير، المحكمة...) يجب أن يكون
**قابلاً للبحث**، لا `DropdownButtonFormField` جامد.

### 1.2 الوضع الحالي
`create_company_wizard.dart:396` وغيره يستخدم `DropdownButtonFormField`
مع `persons.map(...)` — قائمة كاملة بلا بحث. مع مئات الأشخاص تصبح
غير عملية.

### 1.3 التنفيذ
| # | الإجراء |
|---|---|
| 1.3.1 | إنشاء `lib/presentation/widgets/common/searchable_picker.dart` — حقل موحّد: بحث فوري + إنشاء سريع + عرض "لا نتائج" |
| 1.3.2 | يدعم `onCreateNew` اختياري لإضافة عنصر جديد دون مغادرة الشاشة |
| 1.3.3 | البحث يشمل: الاسم، الرقم الوطني، الهاتف (للأشخاص) — وأي حقل يُمرَّر عبر `searchOn` |
| 1.3.4 | استبدال كل `DropdownButtonFormField` مرجعي في ويزارد الدعوى والشركة والوكالة والعقد |
| 1.3.5 | البحث غير حساس للتشكيل وألف/همزة (`أ إ آ ا`) و`ة/ه` و`ى/ي` |

### 1.4 معيار القبول
كتابة جزء من الاسم يُظهر النتائج فوراً؛ عدم وجود نتيجة يعرض زر "إضافة جديد".

---

## المرحلة 2 — تصحيح بنية المحاكم

### 2.1 الوضع الحالي (خاطئ)
`database.dart:571-580` يحقن أسماء مثل:
- `محكمة البداية المدنية الأولى بدمشق`
- `محكمة الاستئناف المدنية بدمشق`
- `محكمة النقض السورية - الغرفة المدنية`

أي: المحافظة + الدرجة + الرقم مدموجة في نص واحد.

### 2.2 المطلوب
| البند | القرار |
|---|---|
| الاسم | **المحافظة فقط** (دمشق، ريف دمشق، حلب، حمص، **حماة**، اللاذقية، طرطوس، إدلب، دير الزور، الحسكة، الرقة، درعا، السويداء، القنيطرة) |
| حقل جديد | **الغرفة** — رقم من 1 إلى 16، مع السماح بإدخال 17، 18... يدوياً |
| يُحذف | كل ما يتعلق بالدرجة: نقض، استئناف، بداية، صلح |
| يُحذف | عبارة "بداية مدنية" من آخر الحقل |
| تصحيح إملائي | "حما" ← **"حماة"** |

### 2.3 التنفيذ
| # | الإجراء |
|---|---|
| 2.3.1 | إضافة عمود `chamberNumber` (INTEGER nullable) لجدول `Courts` — ضمن ترحيل v4 |
| 2.3.2 | إعادة كتابة `_seedDefaultLookups`: 14 محافظة فقط |
| 2.3.3 | ترحيل بيانات: استخراج المحافظة من الأسماء القديمة وتنظيفها |
| 2.3.4 | واجهة الاختيار: محافظة (بحث) + رقم غرفة (إدخال حر بحد أدنى 1) |
| 2.3.5 | إزالة `type` الدرجة من الواجهة (يبقى العمود للتوافق التاريخي) |

> ⚠️ **يحتاج تأكيدك:** جدول `JudicialPhases` (`schema.dart:286`) يمثّل
> تسلسل التقاضي (بداية ← استئناف ← نقض) وهو منطق قانوني مستقل عن
> تسمية المحكمة. هل نُبقيه كما هو ونحذف الدرجة من **اسم المحكمة فقط**؟

---

## المرحلة 3 — رقم الدعوى التلقائي

### 3.1 الوضع الحالي (خاطئ)
`create_case_wizard.dart:944-948` يترك "رقم الدعوى" للمستخدم،
و`:1766` يمرّر `internalNumber: 'TMP'`.

بينما `SequenceService.generateNextInternalNumber()` موجود وجاهز
ويولّد `2026/001` بأمان عبر قفل معاملة.

### 3.2 المطلوب
- الرقم **يُولَّد تلقائياً** ويُعرض للقراءة فقط
- يمثّل **عدد الدعاوى الجارية في المكتب**
- يشمل: المُدخلة من أرشيف الدعاوى الجارية + كل ما يُضاف بعد التثبيت
- **عداد منفصل للمنتهية**
- عند انتهاء دعوى ← ينعكس على العدّادين

### 3.3 التنفيذ
| # | الإجراء |
|---|---|
| 3.3.1 | استدعاء `generateNextInternalNumber()` عند فتح الويزارد وعرضه معطّلاً |
| 3.3.2 | حذف `'TMP'` نهائياً |
| 3.3.3 | إضافة `activeCasesCountProvider` و`closedCasesCountProvider` |
| 3.3.4 | نص توضيحي أسفل الحقل: «هذا الرقم هو تسلسل الدعوى ضمن دعاوى المكتب الجارية (حالياً: N جارية / M منتهية)» |
| 3.3.5 | عند `terminateCase` ← `ref.invalidate` للعدّادين |

> ⚠️ **يحتاج تأكيدك:** إذا انتهت الدعوى رقم 5 من أصل 10، هل يبقى
> رقمها 5 (تسلسل تاريخي ثابت — الأسلم قانونياً) أم يُعاد ترقيم
> الجارية؟ **توصيتي: يبقى ثابتاً**، والعدّاد المعروض هو إحصاء منفصل.

---

## المرحلة 4 — دمج عنوان الدعوى والموضوع

### 4.1 الوضع الحالي
`create_case_wizard.dart:72-73` — حقلان منفصلان:
`_titleController` و`_subjectController`، والخطوة 4 اسمها
"الموضوع والطلبات" (`:298`).

### 4.2 التنفيذ
- دمجهما في حقل واحد: **«موضوع الدعوى»**
- الإبقاء على عمود `subject` في قاعدة البيانات كمصدر وحيد
- ترحيل: `subject = subject ?? title` للسجلات القديمة
- تعديل عرض الدعوى ليقرأ من حقل واحد

---

## المرحلة 5 — المرفقات: الفتح والصيغ المدعومة

### 5.1 الوضع الحالي
`OpenFilex.open` مستخدم في `contract_detail_screen` و
`legal_templates_screen` فقط. شاشة إضافة المرفق **لا** توفّر فتحاً،
والامتدادات غير موحّدة بين الشاشات.

### 5.2 الصيغ المقترحة (تحتاج إقرارك)
| الفئة | الامتدادات | التبرير |
|---|---|---|
| مستندات | `pdf` | الأهم — معاينة داخلية عبر `PdfPreview` المتوفر |
| Word | `doc`, `docx`, `rtf` | صيغ العقود والمذكرات |
| صور | `jpg`, `jpeg`, `png`, `webp`, `heic` | صور الهوية وسندات التوكيل |
| مسح ضوئي | `tif`, `tiff` | مخرجات الماسحات الشائعة |
| نصوص | `txt` | ملاحظات |
| جداول | `xls`, `xlsx`, `csv` | كشوف مالية |

**يُمنع رفع** ما عداها، مع رسالة واضحة تذكر الصيغ المقبولة.

### 5.3 التنفيذ
| # | الإجراء |
|---|---|
| 5.3.1 | ثابت موحّد `AppConstants.allowedAttachmentExtensions` يُستخدم في كل `FilePicker` |
| 5.3.2 | التحقق من الامتداد **بعد** الاختيار أيضاً (لا نثق بالحوار وحده) |
| 5.3.3 | حد أقصى للحجم (مقترح: 50 ميجابايت) مع رسالة واضحة |
| 5.3.4 | زر «فتح» + «إظهار في المجلد» في شاشة إضافة المرفق مباشرة بعد الرفع |
| 5.3.5 | نفس الزرّين في كل شاشة تعرض مرفقاً (الدعوى، الشركة، العقد، الوكالة، الأرشيف) |
| 5.3.6 | معاينة داخلية للـ PDF والصور؛ البقية عبر `OpenFilex` |
| 5.3.7 | معالجة الملف المفقود: رسالة «الملف غير موجود في مساره» بدل فشل صامت |

---

## المرحلة 6 — إصلاحات ويزارد الشركة

### 6.1 المشاكل المؤكدة بالكود

| # | المشكلة | الموقع | التشخيص |
|---|---|---|---|
| 6.1.1 | نص أبيض على خلفية فاتحة في مسار التأسيس | `:121` | لون غير محدد صراحة |
| 6.1.2 | الشكل القانوني لا يتبع الاختيار الأول | `:128, :200` | الخطوتان مستقلتان بلا ربط |
| 6.1.3 | تبويب «عدد الشركاء» | `:143` | التسمية خاطئة ← «الشركاء» |
| 6.1.4 | لا يمكن إضافة شريك إلا من سجل الأشخاص | `:396` | `DropdownButtonFormField` من `allPersonsProvider` فقط |
| 6.1.5 | يُضاف شريك بلا حصة/نسبة | `:379-380` | `double.tryParse(...) ?? 0` يبتلع الفراغ |
| 6.1.6 | لا خيار لإضافة مدير جديد | `:437` | نفس مشكلة الشركاء |
| 6.1.7 | المدير يُضاف فارغاً ويختفي الاختيار | `:448-460` | `_tempDirectorPersonId = null` بلا تحقق كافٍ |
| 6.1.8 | «اسم الشركة موجود مسبقاً» خاطئ | `:541, :635` | يحتاج فحص منطق المطابقة |
| 6.1.9 | تعذّر الرجوع للخطوات السابقة | `:92, :110` | `onStepCancel` مشروط بـ `_currentStep > 0` — يحتاج تتبع |

### 6.2 التنفيذ
| # | الإجراء |
|---|---|
| 6.2.1 | تحديد `style: TextStyle(color: AppColors.textPrimary)` صراحةً لكل عناصر الاختيار |
| 6.2.2 | ربط الشكل القانوني بمسار التأسيس: يُضبط تلقائياً ويُعرض للقراءة مع زر «تغيير» واعٍ |
| 6.2.3 | إعادة تسمية التبويب إلى «الشركاء» |
| 6.2.4 | **نموذج إضافة شريك جديد**: الاسم الثلاثي (مطلوب)، الهاتف، البريد، عنوان الإقامة — يُنشئ `Person` ويُربط فوراً |
| 6.2.5 | تحقق إلزامي: قيمة الحصة **و** النسبة مطلوبتان وأكبر من صفر؛ منع الإضافة مع إبراز الحقل الناقص |
| 6.2.6 | تحذير إن لم يبلغ مجموع النسب 100% |
| 6.2.7 | نفس نموذج الإضافة للمدير + التحقق من اختيار الشخص قبل الإضافة |
| 6.2.8 | عرض **اسم** الشريك/المدير بدل `ID: 12` |
| 6.2.9 | فحص التكرار: مطابقة بعد تطبيع المسافات والهمزات، ورسالة تذكر الشركة المطابقة |
| 6.2.10 | ضمان `onStepCancel` يعمل من كل خطوة مع حفظ المدخلات |
| 6.2.11 | التحقق من انطباق هذا الويزارد على كل أنواع الشركات |

---

## ترتيب التنفيذ

```
المرحلة 0  (الترحيل)        ← أساس، يجب أن يسبق كل شيء
    ↓
المرحلة 2 (المحاكم) + 3 (الترقيم) + 4 (دمج الحقول)   ← تعتمد على ترحيل v4
    ↓
المرحلة 1  (البحث)          ← عنصر مشترك يُستخدم في كل الشاشات
    ↓
المرحلة 6  (ويزارد الشركة)  ← يستهلك عنصر البحث
    ↓
المرحلة 5  (المرفقات)       ← مستقلة، يمكن تنفيذها بالتوازي
```

---

## نقاط تحتاج قرارك قبل التنفيذ

1. **الغرف والدرجات** — هل نحذف الدرجة من اسم المحكمة فقط ونُبقي جدول
   `JudicialPhases` (تسلسل التقاضي) كما هو؟
2. **ترقيم الدعاوى** — هل يبقى رقم الدعوى ثابتاً بعد انتهائها
   (توصيتي: نعم) أم يُعاد ترقيم الجارية؟
3. **صيغ المرفقات** — هل قائمة 5.2 مناسبة؟ أي إضافة أو حذف؟
4. **أنواع الشركات** — هل ويزارد واحد لكل الأنواع أم لكل نوع مسار؟
