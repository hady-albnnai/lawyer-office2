# خطة التسليم 100% — مكتب المحامي V6.2

> التاريخ: 2026-07-10  
> الهدف: منتج قابل لتسليم زبون حقيقي (Windows Offline) بدون Placeholder وبدون أزرار وهمية.  
> المعيار الأعلى: `docs/PRODUCT_REDESIGN_MASTER_PLAN.md` §16.

---

## 1) تعريف الاكتمال (Definition of Done للتسليم)

لا يُسلَّم التطبيق إلا إذا:

1. لا توجد شاشات Placeholder ظاهرة للمستخدم.
2. لا يوجد زر يعرض SnackBar نجاح دون حفظ/تنفيذ حقيقي.
3. لوحة اليوم تعرض بيانات حقيقية من SQLite (جلسات/مهام/نواقص/أوامر عمل).
4. «عمل جديد» يفتح مسارات إنشاء حقيقية (دعوى/عقد/شركة/إجراء/وكالة/أمر عمل).
5. أوامر العمل: إنشاء، طباعة PDF، نص واتساب، إدخال نتيجة، اعتماد — كلها تكتب في DB.
6. تسجيل نتيجة عمل/جلسة يحدّث الحالة ويمكن أن يولّد موعدًا قادمًا.
7. معالج أول تشغيل: مكتب فاضي + اسم المكتب/المحامي + كلمة مرور الزبون.
8. بناء Windows release + توثيق تشغيل صفحتين.
9. اختبارات آلية + Windows CI ناجحة.
10. التوثيق لا يدّعي اكتمالًا غير موجود.

---

## 2) حزم التنفيذ

### الحزمة A — إزالة Placeholder ومسارات الدخول (P0)
- [x] لوحة اليوم الحقيقية
- [x] عمل جديد الحقيقي
- [x] ربط الملاحة/المسارات
- [x] معالج أول تشغيل

### الحزمة B — أوامر العمل التشغيلية (P0)
- [x] إنشاء أمر عمل → DB
- [x] تحديث الحالة (مسودة/مطبوع/مرسل/بانتظار/نتيجة/اعتماد)
- [x] PDF حقيقي لأمر العمل
- [x] توليد نص واتساب + فتح/نسخ
- [x] إدخال نتيجة + اعتماد مع حفظ

### الحزمة C — الأجندة/النتائج (P0)
- [x] تسجيل نتيجة عمل يحفظ فعليًا
- [x] ربط لوحة اليوم بالجلسات/المهام/الأوامر من DB

### الحزمة D — التسليم (P0)
- [x] دليل تشغيل الزبون
- [x] اختبارات وحدة للمسارات الحرجة
- [x] رفع GitHub + CI

### الحزمة E — تحسينات v1.1 (بعد التسليم الأدنى)
- [x] توحيد Sidebar الجديد بالكامل كتجربة وحيدة
- [x] أتمتة أعمق بعد اعتماد أمر العمل (متابعة/مصروف/خط زمني)
- [x] خطوط Cairo/Amiri مضمّنة كأصول محلية
- [x] سكربت حزمة Windows + Inno Setup (التوقيع اختياري عند الناشر)

---

## 3) ترتيب التنفيذ في هذه الجولة

1. توسيع WorkOrderRepository (تحديث حالات/نتائج).
2. إعادة بناء work_order_dialogs لتعمل على DB + PDF + واتساب.
3. بناء TodayDashboard من بيانات حقيقية.
4. بناء NewWorkScreen كمركز إنشاء.
5. معالج أول تشغيل + بوابة في main/app.
6. إصلاح result entry /agenda save path.
7. اختبارات + توثيق + push.

---

## 4) حالة التنفيذ

**آخر تحديث:** 2026-07-10  
**الحالة:** P0 + توحيد الملاحة + مكتب فاضي افتراضي + وثائق التسليم — جاهز لبناء Release + حزمة اختيارية (خطوط/أتمتة/مثبّت).

---

## ما بعد v1.0 — إكمال كل المسارات

الخطة التفصيلية للفجوات المتبقية (F1→F8):

- `FULL_PATHS_COMPLETION_PLAN.md`
- `docs/FULL_PATHS_COMPLETION_PLAN.md`
