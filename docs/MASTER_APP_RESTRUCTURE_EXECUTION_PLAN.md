# الخطة التنفيذية الرئيسية لإعادة هيكلة تطبيق ميزان

> **الغرض:** تجميع كل القرارات المعتمدة في خطة تنفيذ واحدة قبل تعديل الكود، حتى يتم التنفيذ بدقة وبلا عودة لتعديلات متضاربة.  
> **النطاق:** إعادة هيكلة الشريط الجانبي، ملفات المكتب، مكتب العمل، الأرشفة، المكتبة القانونية، النماذج، المالية، التقارير، إدارة المكتب، ثم المساعد الذكي لاحقاً.  
> **شرط التنفيذ:** لا تُعتبر أي مرحلة مكتملة إلا بعد التحليل، الاختبار، التوثيق، والرفع.

---

## 1. الشروط الصارمة

1. لا تغيير في أي منطق خارج نطاق المرحلة المنفذة.
2. لا حذف لجداول أو بيانات حالية قبل وجود بديل مستقر.
3. لا حذف لمسارات قديمة فوراً؛ تُخفى من الواجهة أو تُحوّل لاحقاً بـ Redirect عند الحاجة.
4. كل تعديل يجب أن يحترم الصلاحيات وسجل المسؤولية.
5. لا إدخال شاشة جديدة بلا دور واضح في الهيكل النهائي.
6. لا تنفيذ المساعد الذكي قبل استقرار الهيكل.
7. كل مرحلة تنتهي بـ:

```bash
flutter clean
flutter pub get
flutter analyze
flutter run -d windows
```

قدر الإمكان في بيئة المستخدم، مع إصلاح أي خطأ فوراً.

---

## 2. الشريط الجانبي النهائي المعتمد

الشريط الجانبي النهائي بعد إعادة الهيكلة:

```text
مكتب العمل
ملفات المكتب
المكتبة القانونية
النماذج القانونية
المالية والصندوق
التقارير والكشوف
إدارة المكتب
```

### 2.1 مكتب العمل

يضم لاحقاً:

```text
اليوم
الغد
الأسبوع
التقويم
أوامر العمل
```

لكن التنفيذ المرحلي يجب ألا ينشئ روابط غير موجودة قبل بناء الشاشات.

### 2.2 ملفات المكتب

بديل `الشؤون القانونية` وبديل شاشات الأشخاص والوكالات والأرشيف الفيزيائي المستقلة.

يقسم إلى:

```text
الملفات الجارية
الملفات المنتهية
```

وداخلهما:

```text
الدعاوى
الإجراءات والمعاملات
العقود
الشركات
الوكالات
```

### 2.3 المكتبة القانونية

قسم مستقل للمراجع القانونية:

```text
القوانين السورية
اجتهادات محكمة النقض
الهيئة العامة
مجلة المحامون
أبحاث ومذكرات
كتب ومراجع
```

### 2.4 النماذج القانونية

قسم مستقل للقوالب:

```text
استيراد نموذج
إنشاء نموذج
إدارة النماذج
توليد مستند من نموذج
```

### 2.5 المالية والصندوق

قسم مستقل بسبب أهميته وحساسيته:

```text
لوحة مالية
اتفاقيات الأتعاب
سندات القبض
المصاريف
ذمم الموكلين
صندوق المكتب
التقارير المالية
```

### 2.6 التقارير والكشوف

يشمل الطباعة المركزية ولا يوجد `مركز الطباعة` كقسم مستقل:

```text
كشوف الدعاوى
كشوف الجلسات
كشوف النواقص
كشوف أوامر العمل
كشوف المالية
لائحة عمل اليوم والغد
تصدير PDF
```

### 2.7 إدارة المكتب

بديل `إعدادات النظام` كتسمية للمحامي:

```text
بيانات المكتب
المستخدمون والصلاحيات
سجل المسؤولية
النسخ الاحتياطي
القوائم المرجعية
```

---

## 3. الأقسام التي تُزال من التنقل الرئيسي

تُزال من الشريط الجانبي النهائي:

```text
الشؤون القانونية
السجلات والجهات
الأشخاص والجهات
الوكالات
الأرشيف الفيزيائي
الموارد والمستندات
المستندات الرقمية
مركز الطباعة كقسم مستقل
إعدادات النظام كتسمية
الإدارة والتقارير كتسمية
```

مع الإبقاء على البيانات والمسارات مؤقتاً حتى يتم نقل الوظائف.

---

## 4. ترتيب التنفيذ البرمجي المرحلي

### المرحلة 0 — تثبيت خط الأساس

- التأكد من بناء آخر نسخة.
- إصلاح أي أخطاء حالية قبل الهيكلة.
- عدم تعديل سلوك وظيفي.

### المرحلة 1 — إعادة تسمية وتنظيم الشريط الجانبي مؤقتاً دون كسر المسارات

الهدف:
- تغيير التسميات بما يوافق الهيكل النهائي.
- إبقاء المسارات القديمة صالحة.
- عدم إنشاء شاشات جديدة كبيرة بعد.

أعمال المرحلة:
- `الشؤون القانونية` → `ملفات المكتب`.
- `الإجراءات الإدارية` → `الإجراءات والمعاملات`.
- إزالة مجموعة `السجلات والجهات` من الشريط الجانبي الرئيسي أو إخفاؤها مرحلياً حسب توفر البدائل.
- `الموارد والمستندات` تُفكك مرحلياً.
- `الإدارة والتقارير` تُفكك إلى `المالية والصندوق` و`التقارير والكشوف` و`إدارة المكتب`.

### المرحلة 2 — بناء شاشة ملفات المكتب الموحدة

الهدف:
- شاشة واحدة بفلاتر:

```text
جارية / منتهية / تحتاج استكمال
دعاوى / إجراءات / عقود / شركات / وكالات
تصنيفات فرعية لكل نوع
```

### المرحلة 3 — دمج الأشخاص والوكالات داخل ملفات المكتب والمعالجات

- لا تبقى الأشخاص والوكالات كأقسام مستقلة.
- تبقى كبيانات أطراف داخل الملفات.
- تعديل معالجات الإضافة لتدعم اختيار/إضافة الأطراف والوكالات داخلياً.

### المرحلة 4 — مكتب العمل اليومي

- تحويل لوحة اليوم إلى مكتب العمل:

```text
اليوم = إدخال النتائج
الغد = التحضير والتوزيع
الأسبوع = رؤية قريبة
التقويم = عرض زمني
```

- دعم المهام المخصصة وقوالب نتائجها لاحقاً.

### المرحلة 5 — مركز إدخال الأرشيف القديم

- دفعات إدخال.
- أرشيف ورقي/إلكتروني/CSV.
- تمييز الجاري والمنتهي.
- صندوق غير مصنف.
- ملفات جارية تحتاج استكمال.

### المرحلة 6 — المكتبة القانونية

- استقلالها عن المستندات.
- تجهيزها للقوانين والاجتهادات والمراجع.

### المرحلة 7 — النماذج القانونية

- استيراد نموذج.
- إنشاء نموذج.
- توليد مستند من نموذج.

### المرحلة 8 — التقارير والكشوف

- دمج الطباعة المركزية.
- إبقاء طباعة الملفات داخل ملفاتها.

### المرحلة 9 — المالية والصندوق

- إعادة هيكلة مالية الملف وذمم الموكلين والصندوق.

### المرحلة 10 — إدارة المكتب والقوائم المرجعية

- إدارة القوائم المرجعية بإضافة/تعديل/تعطيل/حذف آمن.

### المرحلة 11 — الاختبار الشامل

- اختبار كل شاشة وكل وظيفة.
- اختبار الصلاحيات.
- اختبار Audit.

### المرحلة 12 — المساعد الذكي

- بعد استقرار كل شيء فقط.

---

## 5. سياسة التنفيذ العملية

كل مرحلة تُنفذ كفرع منطقي داخلي وcommit واضح، ولا نخلط أكثر من نطاق كبير في commit واحد إلا عند الضرورة.

مثال commits:

```text
refactor(nav): align sidebar labels with office structure
feat(files): add office files unified shell
feat(work): add daily work center shell
feat(archive): add archive intake center shell
```

---

## 6. أول مرحلة تنفيذ فعلية بعد هذه الخطة

تبدأ بـ:

```text
المرحلة 1 — إعادة تسمية وتنظيم الشريط الجانبي مؤقتاً دون كسر المسارات
```

لأنها الأقل خطراً، وتثبت لغة التطبيق الجديدة قبل بناء الشاشات الكبيرة.

---

## سجل تنفيذ فعلي — المرحلة 1: تنظيم الشريط الجانبي 2026-07-15

تم تنفيذ المرحلة الأولى من إعادة الهيكلة البرمجية دون كسر المسارات القديمة:

- تغيير مجموعة `الشؤون القانونية` إلى `ملفات المكتب`.
- إضافة `كل الملفات` كمدخل أول ضمن ملفات المكتب عبر المسار الحالي `/files`.
- تنظيم عناصر ملفات المكتب كالتالي:
  - الدعاوى
  - الإجراءات والمعاملات
  - العقود
  - الشركات
  - الوكالات
- إزالة مجموعة `السجلات والجهات` من التنقل الرئيسي، مع إبقاء المسارات القديمة صالحة داخلياً.
- إزالة مجموعة `الموارد والمستندات` من التنقل الرئيسي.
- جعل `المكتبة القانونية` قسماً مستقلاً.
- جعل `النماذج القانونية` قسماً مستقلاً مؤقتاً عبر مسار قوالب العقود الحالي لحين بناء شاشة النماذج القانونية العامة.
- جعل `المالية والصندوق` قسماً مستقلاً.
- جعل `التقارير والكشوف` قسماً مستقلاً ويضم البحث والتقارير والطباعة والتصدير.
- تغيير `إعدادات النظام` إلى `إدارة المكتب`.
- تحديث منطق تحديد العنصر النشط في الشريط الجانبي وفق الهيكل الجديد.
- تحديث فلترة عناصر الشريط حسب الصلاحيات بما يناسب الهيكل الجديد.

> ملاحظة: هذه مرحلة تنظيم تنقل وتسميات فقط. لم يتم حذف أي جداول أو مسارات قديمة، ولم يتم نقل وظائف الأشخاص/الوكالات/الأرشيف الفيزيائي نهائياً بعد. ذلك سيتم في المراحل اللاحقة حسب الخطة.

---

## سجل تنفيذ فعلي — المرحلة 2: بداية شاشة ملفات المكتب الموحدة 2026-07-15

تم تنفيذ خطوة أولى من المرحلة الثانية دون حذف الشاشات القديمة:

- توسيع مزود `uiFilesProvider` ليجمع ملفات المكتب من مصادر حقيقية متعددة:
  - الدعاوى
  - العقود
  - الشركات
  - الإجراءات والمعاملات
  - الوكالات
- إعادة تسمية شاشة `/files` إلى `ملفات المكتب`.
- تحديث تبويب `عاملة` إلى `جارية` بما يوافق مصطلحات الهيكلة الجديدة.
- جعل بطاقة الملف تفتح المسار الحقيقي حسب نوع الملف:
  - دعوى → `/cases/:id`
  - عقد → `/contracts/:id`
  - شركة → `/companies/:id`
  - إجراء → `/procedures/:id`
  - وكالة → `/poa/:id`
- إبقاء تبويبات الحالة الحالية مؤقتاً لحين بناء الواجهة النهائية ذات طبقات:
  - جارية / منتهية / تحتاج استكمال
  - النوع
  - التصنيف الفرعي

> هذه خطوة تأسيسية لشاشة ملفات المكتب، وليست النسخة النهائية الكاملة. الهدف الحالي هو توحيد مصادر الملفات وفتح كل ملف من مكان واحد دون كسر المسارات الحالية.

### سجل تنفيذ فعلي — تحسين واجهة ملفات المكتب 2026-07-15

تم تحسين واجهة `/files` لتصبح أقرب إلى هيكل `ملفات المكتب` المعتمد:

- استبدال تبويبات الملفات القديمة بواجهة فلاتر واضحة:
  - الملفات الجارية
  - الملفات المنتهية
  - تحتاج استكمال
  - كل ملفات المكتب
- إضافة فلتر نوع الملف:
  - دعوى
  - عقد
  - شركة
  - إجراء إداري
  - وكالة
- إضافة بحث موحد برقم الملف أو الاسم أو المحكمة/الجهة أو رقم الأساس/القيد.
- إضافة شريط ملخص يعرض:
  - المعروض
  - الجارية
  - المنتهية
  - تحتاج استكمال
- إبقاء بطاقة الملف تفتح المسار الحقيقي حسب نوع الملف.
- إبقاء التبويبات القديمة غير مستخدمة مؤقتاً في الكود إلى حين تنظيف نهائي بعد استقرار الهيكلة.

### سجل تنفيذ فعلي — تصنيفات فرعية داخل ملفات المكتب 2026-07-15

تمت إضافة طبقة التصنيف الفرعي إلى شاشة ملفات المكتب:

- إضافة حقل `subCategory` إلى نموذج `FileItem`.
- تعبئة التصنيف الفرعي من المصدر الحقيقي:
  - الدعاوى: نوع الدعوى.
  - العقود: نوع العقد.
  - الشركات: الشكل القانوني.
  - الإجراءات: نوع الإجراء.
  - الوكالات: نوع الوكالة.
- إضافة فلتر `كل التصنيفات` في شاشة ملفات المكتب، ويتغير حسب نوع الملف المختار.
- إظهار التصنيف الفرعي كبطاقة صغيرة داخل بطاقة الملف.

هذا يطبق مبدأ الطبقات المتفق عليه:

```text
الحالة ← نوع الملف ← التصنيف الفرعي
```

دون إنشاء شاشات منفصلة أو تكرار واجهات.

### سجل تنفيذ فعلي — ضبط شاشة عمل جديد وفق الهيكل الجديد 2026-07-15

تمت مواءمة شاشة `عمل جديد` مع قرار دمج الأشخاص والمستندات داخل ملفات المكتب والمعالجات:

- إزالة بطاقة `شخص / جهة` كعمل مستقل، لأن إضافة الأشخاص والجهات يجب أن تتم من داخل ملفات الدعوى/الإجراء/العقد/الشركة/الوكالة أو عبر البحث الداخلي.
- إزالة بطاقة `مستند` كعمل مستقل، لأن المستندات أصبحت طبقة مرفقات داخل ملفات المكتب ومركز الأرشيف وليست مسار عمل مستقل.
- إعادة تسمية `وكالة` إلى `ملف وكالة` وربطها بسياق ملفات المكتب.
- تحديث تمييز الشريط الجانبي بحيث تندرج المسارات القديمة الداخلية مثل `/persons` و`/archive` تحت `ملفات المكتب` إذا فُتحت مباشرة.

### سجل تنفيذ فعلي — توحيد مسارات النماذج والتقارير 2026-07-15

تم تنفيذ خطوة تنظيمية إضافية ضمن إعادة الهيكلة:

- تحويل المسار القديم `/contracts/templates` إلى Redirect نحو `/templates` حتى لا تبقى قوالب العقود كمسار مستقل عن `النماذج القانونية`.
- حماية المسار القديم بنفس صلاحية `templates.view`.
- تحديث عنوان شاشة `البحث والتقارير` إلى `التقارير والكشوف`.
- تحديث تبويب `التقارير` إلى `الكشوف والتقارير`.
- تحديث شاشة الطباعة القانونية إلى `الطباعة والتصدير` ضمن مفهوم التقارير والكشوف.

> هذه الخطوة تحفظ التوافق مع المسارات القديمة دون إبقاء التسمية القديمة أو ربط النماذج بالعقود فقط.

### سجل تنفيذ فعلي — مواءمة شاشة إدارة المكتب 2026-07-15

تمت مواءمة شاشة الإعدادات مع تسمية `إدارة المكتب` المعتمدة في الشريط الجانبي:

- تغيير عنوان الشاشة إلى `إدارة المكتب`.
- إعادة ترتيب التبويبات حسب الأولوية الإدارية:
  - بيانات المكتب
  - الأمان
  - المستخدمون والصلاحيات
  - سجل المسؤولية
  - النسخ الاحتياطي
  - القوائم المرجعية
- إخفاء تبويب `سجل النشاط` القديم من الواجهة الرئيسية، مع إبقاء الكود الداخلي مؤقتاً إلى حين استقرار سجل المسؤولية النهائي.

> الهدف هو جعل إدارة المكتب مركزاً واضحاً للضبط والإدارة، وليس شاشة إعدادات تقنية متفرعة.

### سجل تنفيذ فعلي — توضيح تبويبي ملفات المكتب ومعالج الأرشيف 2026-07-17

تمت مواءمة واجهة `ملفات المكتب` مع القاعدة العملية الجديدة:

- التبويب الرئيسي أصبح واضحاً بين:
  - `الملفات الجارية`: تؤثر على مكتب العمل والمواعيد القادمة.
  - `الملفات المنتهية`: للحفظ والبحث فقط.
- تم تعديل الشريط الجانبي ليعرض هذين المدخلين مباشرة تحت `ملفات المكتب` قبل `إدخال الأرشيف القديم`.
- تم بناء بداية معالج إدخال الأرشيف القديم وفق مسار: حالة الأرشيف ← نوع الملف ← التصنيف التفصيلي ← الوثائق ← شاشة الإدخال الرسمية.

> الهدف هو منع خلط الأرشيف المنتهي بالملفات الحية التي يجب أن تغذي مكتب العمل.

### سجل تنفيذ فعلي — ربط معالج الأرشيف بالشاشات الرسمية 2026-07-17

تمت إضافة طبقة `ArchiveEntryContext` لتمرير سياق الأرشيف من معالج الإدخال إلى شاشات الإنشاء الرسمية. الهدف هو أن يشعر المستخدم أنه يعمل ضمن أرشيف جارٍ أو منتهٍ، مع الحفاظ على مسارات الحفظ الأصلية وعدم إنشاء منطق موازٍ.

الشاشات التي أصبحت تستقبل سياق الأرشيف:

- الدعوى.
- الشركة.
- الإجراء / المعاملة.
- العقد.

الملف الجاري يبقى قابلاً لتوليد مواعيد في مكتب العمل من نفس حقول الشاشة الرسمية، أما الملف المنتهي فيظهر تنبيه واضح أنه للحفظ والبحث فقط.

### سجل تنفيذ فعلي — مرونة الإضافات غير المعروفة 2026-07-17

تم تطبيق مبدأ أن المستخدم يستطيع إضافة ما يحتاجه حتى لو لم يكن معرفاً مسبقاً:

- أنواع ملفات جديدة داخل معالج الأرشيف.
- أنواع دعاوى ومحاكم/درجات جديدة.
- أنواع شركات وإجراءات وعقود ووكالات.
- أسماء وثائق مخصصة.
- أرشيف غير محدد مع رفع ملفات وتحويلها لصندوق مراجعة.
- حقول الأصل الورقي عند رفع المستندات.

تم تنفيذ ذلك دون كسر الشاشات الرسمية أو إنشاء منطق حفظ موازٍ؛ المعالج يوجّه، والشاشات الرسمية أو دفعات الأرشيف تحفظ.

### سجل تنفيذ فعلي — منع أثر الأرشيف المنتهي على العمل القادم 2026-07-17

تم تعزيز الفصل بين الأرشيف الجاري والمنتهي داخل شاشات الإدخال:

- الدعوى القادمة من أرشيف منتهٍ لا تطلب موعداً قادماً وتحفظ بحالة مغلقة.
- الشركة القادمة من أرشيف منتهٍ تحفظ كأرشيف.
- الوكالة القادمة من أرشيف منتهٍ تحفظ كأرشيف.
- الإجراء والعقد يحتفظان بمنطق عدم إنشاء موعد/تذكير عند الأرشيف المنتهي.

كما تمت إضافة أزرار إدخال التصنيفات غير المعروفة داخل الشاشات الرسمية نفسها.

### سجل تنفيذ فعلي — استكمال فصل الجاري عن المنتهي 2026-07-17

تمت مراجعة أثر الأرشيف المنتهي على الحالات العملية للملفات، وتم تثبيت حفظ الحالات بما يجعلها تظهر ضمن `الملفات المنتهية` لا `الملفات الجارية` قدر الإمكان، مع السماح بأنواع مستندات مخصصة في شاشة المستندات.

### سجل تنفيذ فعلي — تنظيف تبويبات ملفات المكتب في الشريط الجانبي 2026-07-17

تمت مواءمة الشريط الجانبي مع القرار النهائي بأن `ملفات المكتب` لا يجب أن يفرش أنواع الملفات كأقسام جانبية مستقلة. أصبح الظاهر تحت `ملفات المكتب` فقط:

- الملفات الجارية.
- الملفات المنتهية.
- إدخال الأرشيف القديم.

تبقى مسارات الدعاوى والشركات والإجراءات والعقود والوكالات موجودة داخلياً وتُفتح من المعالجات، البحث، بطاقات ملفات المكتب، ومركز الأرشيف، لكنها لم تعد تبويبات جانبية مستقلة حتى لا تختلط بنية الأرشيف مع طريقة التصنيف الداخلية.

### سجل تنفيذ فعلي — تحسين تنقل تبويبي ملفات المكتب 2026-07-17

تم تحسين شاشة `ملفات المكتب` بعد تنظيف الشريط الجانبي:

- عند الانتقال بين `الملفات الجارية` و`الملفات المنتهية` من الشريط الجانبي، تتحدث حالة الشاشة تلقائياً حتى لو كانت الشاشة مفتوحة مسبقاً.
- تمت إضافة زر مباشر في شريط شاشة ملفات المكتب لفتح `إدخال الأرشيف القديم` لمن يملك الصلاحية.

> هذا يجعل تبويبي الجاري والمنتهي مدخلين عمليين واضحين وليس مجرد فلاتر داخلية.

### سجل تنفيذ فعلي — تثبيت إبراز تبويبي الجاري والمنتهي 2026-07-17

تم تحسين تنقل `ملفات المكتب` بعد اعتماد مدخلي `الملفات الجارية` و`الملفات المنتهية`:

- صار الشريط الجانبي يميز التبويب المختار فعلياً بحسب رابط `status`.
- عند تغيير التبويب من داخل شاشة ملفات المكتب يتغير الرابط أيضاً، حتى يبقى التنقل واضحاً وقابلاً للحفظ والرجوع.
- تم تحسين حالة الفراغ:
  - في الملفات الجارية يظهر توجيه لإدخال أرشيف جارٍ أو إضافة عمل جديد.
  - في الملفات المنتهية يظهر توجيه لإدخال أرشيف منتهٍ.

> هذا يجعل مدخلي الجاري والمنتهي واضحين للمستخدم كمسارين عمليين لا كفلاتر مخفية.

### سجل تنفيذ فعلي — توجيه إدخال الأرشيف حسب تبويب ملفات المكتب 2026-07-17

تم ربط زر `إدخال الأرشيف القديم` داخل شاشة `ملفات المكتب` بالتبويب الحالي:

- من تبويب `الملفات الجارية` يفتح المعالج مع اختيار `أرشيف جارٍ` مسبقاً.
- من تبويب `الملفات المنتهية` يفتح المعالج مع اختيار `أرشيف منتهٍ` مسبقاً.

هذا يقلل خطوة على المستخدم ويمنع الخلط بين الأرشيف الذي يغذي مكتب العمل والأرشيف الذي يبقى للحفظ والبحث فقط.

### سجل تنفيذ فعلي — إبراز أنواع الملفات داخل تبويبي الجاري والمنتهي 2026-07-17

بعد إزالة أنواع الملفات من الشريط الجانبي، تم تحسين شاشة `ملفات المكتب` بحيث تصبح أنواع الملفات واضحة داخل الشاشة نفسها:

- إضافة شريط اختيارات سريع لأنواع الملفات:
  - كل الأنواع.
  - دعوى.
  - عقد.
  - شركة.
  - إجراء إداري.
  - وكالة.
- يبقى فلتر التصنيف الفرعي موجوداً ويتغير حسب نوع الملف المختار.
- عند فتح معالج الأرشيف من تبويب الجاري أو المنتهي، صار تغيير الرابط يعيد ضبط نوع الملف والتصنيفات السابقة حتى لا يحمل المستخدم اختياراً قديماً بالخطأ.

> بهذا صار الشريط الجانبي بسيطاً، بينما بقيت قوة التصنيف داخل شاشة ملفات المكتب والمعالج.

### سجل تنفيذ فعلي — تمرير نوع الملف من ملفات المكتب إلى معالج الأرشيف 2026-07-17

تم تحسين الربط بين شاشة `ملفات المكتب` ومعالج `إدخال الأرشيف القديم`:

- إذا كان المستخدم داخل تبويب الجاري/المنتهي واختر فلتر نوع ملف مثل `دعوى` أو `شركة` ثم ضغط إدخال الأرشيف، يفتح المعالج مع النوع المحدد مسبقاً.
- الربط الحالي:
  - دعوى ← مسار دعوى.
  - عقد ← مسار عقد.
  - شركة ← مسار شركة.
  - إجراء إداري ← مسار إجراء.
  - وكالة ← مسار وكالة.
- إذا لم يكن هناك نوع محدد، يفتح المعالج على حالة الأرشيف فقط ويترك اختيار النوع للمستخدم.

> هذا يقلل خطوات الإدخال ويجعل شاشة ملفات المكتب بوابة عملية للأرشيف حسب السياق الحالي.

### سجل تنفيذ فعلي — إبراز مدخل إدخال الأرشيف في الشريط الجانبي 2026-07-17

تم ضبط تمييز الشريط الجانبي بحيث عند فتح `/archive-intake` يظهر عنصر `إدخال الأرشيف القديم` نفسه كمحدد داخل `ملفات المكتب`، بدلاً من تمييز `ملفات المكتب` فقط.

> هذا يحسن وضوح التنقل بعد اعتماد الجاري/المنتهي/إدخال الأرشيف كالمداخل الثلاثة الوحيدة تحت ملفات المكتب.

### سجل تنفيذ فعلي — منع إظهار نواقص تشغيلية على الملفات المنتهية 2026-07-17

تم تحسين بطاقات `ملفات المكتب` بعد تثبيت فصل الجاري عن المنتهي:

- عداد `تحتاج استكمال` صار يحسب الملفات الجارية فقط.
- بطاقات الملفات المنتهية لم تعد تعرض تحذيرات تشغيلية مثل `بانتظار رقم أساس` أو `مستندات ناقصة` كعمل مطلوب.
- يظهر بدل ذلك توضيح أن الملف محفوظ للأرشيف والبحث فقط.

> هذا يمنع خلط الأرشيف المنتهي مع ملفات العمل التي تحتاج متابعة أو استكمال.

### سجل تنفيذ فعلي — إضافة إدخال الأرشيف إلى الإجراء السريع 2026-07-17

تمت إضافة `إدخال الأرشيف القديم` إلى قائمة `إضافة سريع` في الشريط العلوي لمن يملك صلاحية عرض مركز الأرشيف.

> الهدف تسهيل بدء إدخال الأرشيف من أي شاشة دون الرجوع دائماً إلى الشريط الجانبي أو شاشة ملفات المكتب.

### سجل تنفيذ فعلي — إضافة الأرشيف الجاري والمنتهي إلى شاشة عمل جديد 2026-07-17

تمت إضافة خيارين واضحين في شاشة `عمل جديد`:

- `إدخال أرشيف جارٍ`: للملفات القديمة الحية التي يجب أن تغذي مكتب العمل والتقويم.
- `إدخال أرشيف منتهٍ`: للملفات القديمة المحفوظة للبحث فقط دون أثر تشغيلي.

> هذا يجعل أول قرار في إدخال الأرشيف متاحاً أيضاً من شاشة العمل الجديد، وليس فقط من ملفات المكتب أو الشريط الجانبي.

### سجل تنفيذ فعلي — فصل الأرشيف الجاري والمنتهي في الإجراء السريع 2026-07-17

تم تحسين قائمة `إضافة سريع` في الشريط العلوي:

- استبدال خيار الأرشيف العام بخيارين واضحين:
  - `إدخال أرشيف جارٍ`: يفتح المعالج محدداً على الأرشيف الجاري.
  - `إدخال أرشيف منتهٍ`: يفتح المعالج محدداً على الأرشيف المنتهي.
- تمت إضافة وصف مختصر لكل خيار يوضح أثره على مكتب العمل.

> هذا يثبت قرار أن أول سؤال في الأرشيف هو: جارٍ أم منتهٍ، حتى من الإجراء السريع.

### سجل تنفيذ فعلي — أوامر الأرشيف في البحث الشامل 2026-07-17

تمت إضافة أوامر سريعة إلى شريط البحث/الأوامر `Ctrl+K`:

- `/ارشيف جاري` يفتح معالج إدخال الأرشيف محدداً على أرشيف جارٍ.
- `/ارشيف منتهي` يفتح معالج إدخال الأرشيف محدداً على أرشيف منتهٍ.

كما تم تنظيف دورة حياة نافذة البحث بإغلاق متحكم النص عند التخلص من الواجهة.

> هذا يجعل الوصول إلى إدخال الأرشيف متاحاً من الشريط الجانبي، الإجراء السريع، شاشة عمل جديد، شاشة ملفات المكتب، والبحث الشامل.

### سجل تنفيذ فعلي — أوامر ملفات المكتب وتحسين تطبيع الأوامر 2026-07-17

تم تحسين أوامر `Ctrl+K` المرتبطة بملفات المكتب والأرشيف:

- إضافة أمر `/ملفات جارية` لفتح تبويب الملفات الجارية مباشرة.
- إضافة أمر `/ملفات منتهية` لفتح تبويب الملفات المنتهية مباشرة.
- تحسين مطابقة الأوامر العربية بحيث لا تتأثر بالفروق بين `أ/إ/آ/ا` أو `ة/ه`.
- بقيت أوامر `/ارشيف جاري` و`/ارشيف منتهي` متاحة لبدء معالج الأرشيف.

> هذا يجعل الوصول السريع لمدخلي الجاري والمنتهي ممكناً من البحث الشامل أيضاً.

### تعديل مسار التنفيذ — أرشيف V2 القانوني 2026-07-17

تم اعتماد خطة جديدة لمسار إدخال الأرشيف القديم بناءً على مراجعة تجربة المحامي:

```text
docs/ARCHIVE_INTAKE_V2_LEGAL_WORKFLOW_PLAN.md
```

قرارات تؤثر على إعادة الهيكلة العامة:

- حصر إدخال الأرشيف في تبويب `إدخال الأرشيف القديم` فقط.
- تحويل `الملفات الجارية` و`الملفات المنتهية` إلى شاشات تعامل وبحث وطباعة وتعديل فقط.
- بناء ويزاردات قانونية متخصصة للدعاوى والأطوار القضائية بدلاً من الاكتفاء بفتح شاشات الإنشاء العامة.
- إنشاء سجل مندوبي الوكالات ككيان مرجعي جديد.
- ربط ملفات الدعاوى المنتهية بأزرار تصعيد قانوني: استئناف، نقض، مخاصمة.

> يجب تنفيذ هذه الخطة على مراحل نقاشية دقيقة قبل كتابة الكود النهائي للويزاردات، منعاً لتكرار تنفيذ واجهات غير مطابقة لسير عمل المحامي.

### تعديل تفصيلي — اعتماد مخطط الأرشيف القانوني السوري قبل التنفيذ 2026-07-17

تم توسيع خطة الأرشيف V2 في `docs/ARCHIVE_INTAKE_V2_LEGAL_WORKFLOW_PLAN.md` بعد مراجعة الهيكل القضائي السوري، وتم تثبيت الآتي كشرط قبل التنفيذ:

- حصر إدخال الأرشيف في تبويب `إدخال الأرشيف القديم` فقط.
- بناء ويزاردات قانونية متخصصة للدعاوى، لا مجرد فتح شاشات الإنشاء العامة.
- دعم درجة أولى، استئناف، نقض، مخاصمة كمسارات مختلفة.
- إضافة سجل مندوبي الوكالات مع أرقام الهواتف وفروع النقابة.
- إضافة أزرار استئناف/نقض/مخاصمة داخل ملفات الدعاوى المنتهية عندما تكون مناسبة.

لن يبدأ تنفيذ كود الويزاردات قبل الاتفاق على أول ويزارد تفصيلياً، منعاً لتكرار تنفيذ غير مطابق لسير العمل القانوني.

### تصحيح تنفيذي — حصر إنشاء الملفات الجديدة في مكتب العمل 2026-07-17

تم اعتماد قرار الملاحة العملي التالي:

- زر إنشاء الملفات الجديدة بعد نزول التطبيق يكون في `مكتب العمل` فقط، باسم `إنشاء جديد`.
- إزالة زر الإضافة من شاشة `ملفات المكتب` حتى تبقى شاشة تعامل وبحث وتعديل وطباعة لا شاشة إنشاء.
- تحويل المسار القديم `/new-work` إلى `/today` منعاً لاستخدام شاشة عمل جديد كمدخل مستقل.
- إزالة قائمة `إضافة سريع` من الشريط العلوي حتى لا تتعدد مداخل الإنشاء.
- إزالة أوامر إنشاء الأرشيف من البحث الشامل، والإبقاء على أوامر فتح الملفات الجارية والمنتهية فقط.

كما تم تعديل تمييز العنصر الرئيسي في الشريط الجانبي حتى لا يظهر النص بلون أبيض فوق خلفية فاتحة عند اختيار تبويب رئيسي أو وجود عنصر فرعي محدد.

### سجل تنفيذ فعلي — سجل مندوبي الوكالات 2026-07-17

تم تنفيذ سجل مندوبي الوكالات كبنية مرجعية مستقلة:

- جدول SQL: `agency_delegates`.
- واجهة إدارة ضمن القوائم المرجعية.
- ربط أولي بنافذة إضافة الوكالة.
- توسيع قائمة فروع النقابة الافتراضية لتشمل فروع المحافظات السورية المتداولة.

> هذه الخطوة مطلوبة قبل بناء ويزارد الدعوى الأرشيفية لأن الوكالة ستحتاج اختيار فرع النقابة ومندوب الوكالات وبيانات الاتصال به.

### ملاحظة مؤجلة — تعديلات الشركات بعد التأسيس 2026-07-17

تم الاتفاق أثناء نقاش ويزارد `إنشاء شركة جديدة` على أن موضوع إدارة الشركة بعد التأسيس لن يُحسم الآن داخل ويزارد الإنشاء، بل يؤجل إلى مرحلة نقاش `الملفات الجارية` وتعديلاتها.

المقصود لاحقاً:

- إدارة تعديلات عقد التأسيس لشركات الأشخاص.
- إدارة تعديلات النظام الأساسي لشركات الأموال.
- إضافة نسخ معدلة من الوثيقة التأسيسية.
- محاضر هيئات عامة أو اجتماعات شركاء.
- قرارات تعديل رأس المال أو المدير أو الغاية أو المقر أو نسب الشركاء.
- ربط هذه التعديلات بسجل الشركة الجارية لا بويزارد الإنشاء الأولي.

> لذلك يقتصر ويزارد إنشاء الشركة حالياً على التأسيس الأولي والوثائق الأولية، بينما تدار التعديلات اللاحقة من ملف الشركة الجاري عند مناقشة شاشات الملفات الجارية.

### قرار تصميمي — أطراف العقود غير محدودة 2026-07-17

تم الاتفاق في نقاش مسار العقود على أن العقد لا يجوز تصميمه بفرض طرف أول وطرف ثاني فقط.

القاعدة المعتمدة:

```text
العقد يحتوي عدداً غير محدود من الأطراف.
كل طرف يحتوي عدداً غير محدود من الأشخاص أو الجهات.
كل شخص/جهة داخل الطرف يمكن أن يكون له صفة خاصة.
```

هذا القرار يجب أن يؤخذ بعين الاعتبار عند إعادة بناء ويزارد `عقد جديد` وعند ربطه بالنماذج القانونية وملفات المكتب.

### قرار تصميمي — ويزارد عقد جديد 2026-07-17

تم اعتماد تصميم مبسط وعملي لمسار العقود:

- أرشفة العقد القديم تتم من `إدخال الأرشيف القديم` فقط.
- إنشاء العقد الجديد يتم من `مكتب العمل > إنشاء جديد > عقد جديد`.
- العقد الجديد يدعم ثلاث طرق:
  - إنشاء من نموذج Word.
  - رفع عقد جاهز.
  - حفظ بدون ملف حالياً.
- أطراف العقد غير محدودة، وكل طرف قد يحتوي عدداً غير محدود من الأشخاص أو الجهات.
- المتابعة في الويزارد تكون تذكيراً واحداً فقط، وتضاف المتابعات الأخرى لاحقاً من ملف العقد الجاري.
- الأتعاب اختيارية ومنفصلة عن قيمة العقد بين الأطراف.

تم توثيق التفاصيل في `docs/ARCHIVE_INTAKE_V2_LEGAL_WORKFLOW_PLAN.md`.

### قرار مصطلحي ملزم — رقم ملف المكتب ورقم أساس الدعوى 2026-07-17

تم الاتفاق على قاعدة مصطلحية وتشغيلية مهمة يجب تطبيقها في كل شاشات الملفات والدعاوى والتقارير:

```text
رقم ملف المكتب ≠ رقم أساس الدعوى
```

#### رقم ملف المكتب

هو الرقم الداخلي الذي ينشئه التطبيق أو المكتب لإدارة الملف داخل المكتب.

أمثلة:

```text
2026/001
C-2026-015
```

استخدامه:

- البحث الداخلي.
- ترتيب ملفات المكتب.
- الطباعة والتقارير.
- ربط المستندات.
- ربط الأتعاب والمصاريف.
- أوامر العمل.
- الأرشيف الداخلي.

التسمية المعتمدة في الواجهة:

```text
رقم ملف المكتب
```

أو عند الضرورة التقنية:

```text
الرقم الداخلي
```

لكن لا يسمى `رقم الدعوى` إذا كان المقصود رقم ملف المكتب.

#### رقم أساس الدعوى

هو الرقم القضائي الصادر عن المحكمة أو المرجع القضائي.

أمثلة:

```text
أساس 120 لعام 2026
أساس استئناف 44 لعام 2027
```

التسمية المعتمدة:

```text
رقم الأساس
سنة الأساس
```

ويجب ربطه بالمرحلة القضائية عند تعدد المراحل:

```text
أساس الصلح
أساس البداية
أساس الاستئناف
أساس التنفيذ
```

#### النقض والمخاصمة

في النقض والمخاصمة قد لا تكون التسمية العملية دائماً `أساس`، لذلك تعتمد تسميات مثل:

```text
رقم الطعن
سنة الطعن
رقم المرجع القضائي
```

حسب المرحلة.

#### أثر القرار على التنفيذ

- يجب مراجعة كل التسميات القديمة التي تقول `رقم الدعوى`.
- إذا كان المقصود رقم المكتب، تستبدل بـ `رقم ملف المكتب`.
- إذا كان المقصود رقم المحكمة، تستبدل بـ `رقم الأساس`.
- في شاشة الدعوى يجب فصل بيانات المكتب عن بيانات المحكمة.
- يجب أن تكون مراحل الدعوى قادرة على حمل أرقام أساس مختلفة لكل مرحلة.

> هذا القرار يمنع الخلط بين إدارة المكتب وبين القيود القضائية الرسمية، وهو شرط أساسي قبل إعادة بناء شاشة الدعوى الجارية.

### قرار تصميمي — شاشة الملفات الجارية 2026-07-17

تم الاتفاق على هيكلة شاشة `الملفات الجارية` بالشكل التالي:

- الشاشة هي لوحة موحدة لكل الملفات الحية التي يعمل عليها المكتب حالياً.
- لا تحتوي مدخلاً لإنشاء أرشيف أو إنشاء ملف جديد؛ الإنشاء الجديد يبقى من `مكتب العمل > إنشاء جديد` فقط.
- تحتوي على بحث شامل في:
  - رقم ملف المكتب.
  - الموضوع / العنوان.
  - الموكل أو الجهة.
  - الخصم عند توفره.
  - المحكمة أو الجهة.
  - رقم الأساس أو المرجع.
- تحتوي فلاتر نوع الملف:
  - كل الملفات.
  - دعاوى.
  - إجراءات / معاملات.
  - شركات.
  - عقود.
  - وكالات.
- تحتوي فلاتر تشغيلية:
  - لديها موعد قريب.
  - متأخرة.
  - تحتاج استكمال.
  - لديها أوامر عمل مفتوحة.
  - بلا مستندات.
- تحتوي مؤشرات علوية:
  - المعروض.
  - دعاوى.
  - إجراءات.
  - شركات.
  - عقود.
  - وكالات.
  - متأخرة.
  - تحتاج استكمال.
- طريقة العرض المعتمدة مبدئياً: بطاقات ملفات مع تصدير/طباعة، وتأجيل عرض الجدول إلى مرحلة لاحقة عند الحاجة.
- ترتيب العرض الافتراضي:
  - الملفات المتأخرة أو الأقرب موعداً أولاً.
  - ثم حسب الموعد القادم.
  - ثم آخر تحديث.
- كل بطاقة ملف تعرض:
  - رقم ملف المكتب.
  - نوع الملف.
  - التصنيف الفرعي.
  - الموضوع.
  - الموكل أو الجهة الأساسية.
  - المحكمة/الجهة أو المرجع.
  - رقم الأساس أو المرجع عند توفره.
  - الموعد القادم والإجراء القادم عند توفرهما.
  - النواقص.
  - عدد المستندات.
  - آخر تحديث.
- الأزرار السريعة داخل البطاقة تكون محدودة:
  - فتح الملف.
  - إضافة أمر عمل.
  - إجراء تشغيلي سريع بحسب النوع عند الحاجة، مثل إدخال نتيجة جلسة للدعوى أو تحديث خطوة للإجراء.
- التعديل الكامل يتم من شاشة تفاصيل الملف المتخصصة حسب نوعه، وليس من بطاقة الملفات الجارية.
- فتح الملف ينتقل إلى شاشة تفاصيله الأصلية:
  - دعوى ← شاشة الدعوى.
  - إجراء ← شاشة الإجراء.
  - شركة ← شاشة الشركة.
  - عقد ← شاشة العقد.
  - وكالة ← شاشة الوكالة.

> هذه الهيكلة تعتمد قائمة موحدة للملفات الجارية مع تفاصيل متخصصة لكل نوع ملف، وهي الأقرب للتنفيذ الحالي والأفضل لتجنب بناء شاشة ضخمة واحدة لكل الأنواع.

### قرار تصميمي — شاشة الدعوى الجارية Workspace 2026-07-17

تم الاتفاق على هيكلة شاشة الدعوى الجارية وفق المبادئ التالية:

- رأس ملف ثابت أعلى الشاشة.
- تبويبات متخصصة لا شاشة طويلة واحدة.
- رقم ملف المكتب منفصل تماماً عن رقم الأساس القضائي.
- المحامي المسؤول يختار من مستخدمي التطبيق، مع دعم محامين مساعدين لاحقاً.
- حالة الدعوى مفصلة:
  - قيد التحضير.
  - مسجلة.
  - قيد التبليغ.
  - قيد المحاكمة.
  - بانتظار قرار.
  - بانتظار تنفيذ.
  - معلقة.
  - مؤجلة.
  - مغلقة / منتهية.
- دعم عدة موكلين وعدة خصوم وصفات متعددة للأطراف.
- الجلسات تكون داخل مراحلها القضائية، ولا تختلط جلسات البداية بالاستئناف أو النقض.
- كل مرحلة تحمل صفة مركبة مثل:
  - صلح مدني.
  - بداية مدنية.
  - صلح جزاء.
  - بداية جزاء.
  - استئناف مدني.
  - نقض جزائي.
- الحقول الثابتة أو الحساسة مثل نوع المرحلة والمحكمة ورقم الأساس وسنته تعرض تنبيهاً عند إدخالها، ولا تعدل لاحقاً إلا من تبويب المراحل.
- زر إغلاق الدعوى يعتمد تسمية أدق:
  - إغلاق / إنهاء مرحلة.
- أسباب الإغلاق:
  - التخلي عن الملف / تسليمه.
  - إسقاط / تنازل / صلح.
  - صدور قرار قضائي.
  - انتهاء التنفيذ.
  - سبب آخر.
- التخلي عن الملف يدعم تصدير صيغة لا مانع قابلة للتعديل.
- النواقص تحذيرية ولا تمنع الحفظ مبدئياً.
- المستندات تعرض كقائمة واحدة مع فلاتر.
- المالية داخل الدعوى تعرض وتسمح بإضافات سريعة حسب الصلاحية.
- أوامر العمل المعتمدة يمكن أن تحدث الملف حسب نتيجتها.
- الطباعة تكون من زر واحد بقائمة خيارات.

تم تفصيل هذه القرارات أيضاً في:

```text
docs/ACTIVE_CASE_WORKSPACE_PLAN.md
```

> هذه الخطة ستكون مرجع تنفيذ شاشة تفاصيل الدعوى الجارية بعد الانتهاء من تثبيت ويزاردات الإنشاء الأساسية.

### قرار تصميمي — آثار الإغلاق والإجراءات اللاحقة للدعاوى 2026-07-18

تم اعتماد القاعدة العملية التالية لشاشة الدعوى الجارية والمنتهية:

```text
شطب الدعوى = مسار تجديد الدعوى، وليس إغلاقاً نهائياً للحق.
رد الدعوى شكلاً = يفتح استئناف/نقض/دعوى جديدة مرتبطة حسب المرحلة، ولا يعامل كخسارة موضوعية تلقائية.
التخلي عن الملف / تسليمه = إغلاق علاقة المكتب بالملف مع دعم لا مانع وإمكانية إعادة فتح الملف لاحقاً إذا عاد للمكتب.
التنازل عن الحق = إغلاق نهائي حساس يتطلب تحذيراً وصلاحية مناسبة ومستنداً أو سبباً واضحاً.
صدور حكم أو قرار = إنهاء مرحلة وفتح التصعيد المناسب: استئناف، نقض، مخاصمة، تنفيذ، أو إغلاق نهائي.
```

أثر القرار على التنفيذ:

- زر الدعوى الأساسي يكون `إغلاق / إنهاء مرحلة` لا `إغلاق الدعوى` فقط.
- الملفات المنتهية يجب أن تحتوي قسماً أو تبويباً باسم `إجراءات لاحقة / طعون`.
- زر `تجديد الدعوى` يظهر عند الشطب أو الحالات الشكلية التي تقبل التجديد أو إعادة القيد بحسب السياق.
- أزرار الاستئناف والنقض والمخاصمة والتنفيذ تظهر بحسب المرحلة والاختصاص، لا كأزرار عامة دائماً.
- التخلي عن الملف يدعم إنشاء وطباعة `لا مانع` قابل للتعديل.
- إعادة فتح ملف منتهٍ إلى جارٍ ممكنة بصلاحية وسبب وسجل أحداث.

تم توثيق التفصيل في:

```text
docs/ACTIVE_CASE_WORKSPACE_PLAN.md
```

### قرار تصميمي — شاشة الدعوى المنتهية 2026-07-18

تم اعتماد أن الدعوى المنتهية تفتح ضمن نفس شاشة الدعوى `Workspace` لكن بوضع منتهي، وليس كشاشة أرشيفية منفصلة.

القرارات:

- نفس شاشة الدعوى مع أغلب الحقول الحساسة بوضع قراءة فقط.
- لا تظهر النواقص التشغيلية المعتادة كما في الدعوى الجارية.
- يضاف تبويب `إجراءات لاحقة / طعون`.
- تبقى تبويبات: الملخص، الأطراف والوكالات، المراحل والجلسات، المستندات، أوامر العمل، المالية، سجل الأحداث.
- إعادة فتح ملف منتهٍ كملف جارٍ تكون للمالك أو المدير المخول فقط، مع سبب إلزامي وسجل أحداث.
- كتاب `لا مانع` يبدأ كنص افتراضي قابل للتعديل، ويصمم بحيث يمكن ربطه لاحقاً بقوالب النماذج القانونية.

تم توثيق التفصيل في:

```text
docs/ACTIVE_CASE_WORKSPACE_PLAN.md
```

### قرارات تشغيلية — مالية الدعوى المنتهية والتصعيد 2026-07-18

تم اعتماد الآتي:

- بعد انتهاء الدعوى تبقى المالية قابلة لإضافة سند قبض أو مصروف مرتبط بالملف فقط.
- تعديل اتفاق الأتعاب بعد الإغلاق ليس إجراءً عادياً، ويحتاج صلاحية إدارية أو مسار تصحيح.
- الاستئناف أو النقض أو المخاصمة من دعوى منتهية يضاف كمرحلة جديدة داخل نفس رقم ملف المكتب.
- كل مرحلة طعن تحمل رقمها القضائي الخاص، مع بقاء رقم ملف المكتب الداخلي واحداً.
- الدعوى الجديدة المرتبطة بعد رد شكلي تحمل رقماً داخلياً تابعاً للرقم القديم مع لاحقة، مثل `2026/001-R` أو صيغة عربية يحددها المكتب لاحقاً.

تم توثيق التفصيل في:

```text
docs/ACTIVE_CASE_WORKSPACE_PLAN.md
```

### قرار تشغيلي — أوامر العمل في الدعوى المنتهية 2026-07-18

تم اعتماد أن أوامر العمل داخل الدعوى المنتهية تكون أوامر لاحقة محدودة فقط، وليست أوامر تشغيلية عادية.

الأوامر المسموحة أمثلة:

```text
طباعة لا مانع
تسليم مستندات
قبض أتعاب
إضافة مصروف مرتبط بالملف
متابعة استئناف / نقض / مخاصمة
تحضير ملف تنفيذ
أرشفة مستند لاحق
تسليم صورة حكم أو قرار
```

إذا احتاج الملف إلى عمل تشغيلي كامل، يجب استخدام مسار واضح مثل إعادة فتح الملف بصلاحية المالك/المدير، أو فتح مرحلة طعن، أو فتح تنفيذ، أو دعوى جديدة مرتبطة.

تم توثيق التفصيل في:

```text
docs/ACTIVE_CASE_WORKSPACE_PLAN.md
```

### قرار تصميمي — ويزارد إغلاق / إنهاء مرحلة الدعوى 2026-07-18

تم اعتماد تصميم ويزارد `إغلاق / إنهاء مرحلة` داخل تبويب `المراحل والجلسات`، ويكون مرتبطاً بالمرحلة الحالية لا بالدعوى كلها فقط.

أسباب الإنهاء المعتمدة:

```text
صدور حكم / قرار قضائي
شطب الدعوى
رد الدعوى شكلاً
إسقاط / تنازل / صلح
انتهاء التنفيذ
التخلي عن الملف / تسليمه
حفظ / تعليق بانتظار قرار الموكل
سبب آخر
```

الويزارد يعرض حقولاً مختلفة حسب السبب، ثم شاشة مراجعة قبل الحفظ، وبعد الحفظ يطبق الأثر المناسب:

```text
إغلاق نهائي
فتح استئناف / نقض / مخاصمة داخل نفس رقم ملف المكتب
فتح تنفيذ
تجديد دعوى مشطوبة
دعوى جديدة مرتبطة بعد رد شكلي
تخلي مع لا مانع
تعليق بانتظار قرار الموكل
```

كل عملية يجب أن تسجل في سجل الأحداث، وكل مستند ناتج أو مرفق يربط بتبويب المستندات.

تم توثيق التفصيل الكامل في:

```text
docs/ACTIVE_CASE_WORKSPACE_PLAN.md
```

### قرار تصميمي — ويزارد دعوى جديدة / دعوى متابعة 2026-07-18

تم اعتماد ويزارد إنشاء الدعوى الجديدة من:

```text
مكتب العمل > إنشاء جديد > دعوى
```

ولا يظهر هذا الويزارد في `الملفات الجارية` أو `الملفات المنتهية`، ولا يستخدم كبداية لإدخال الأرشيف القديم.

أول سؤال في الويزارد:

```text
دعوى جديدة
دعوى متابعة
```

مع إيضاح بسيط تحت كل خيار حتى لا يختار المستخدم عشوائياً:

- `دعوى جديدة`: عندما يبدأ المكتب الدعوى من بدايتها ولا توجد جلسات أو إجراءات قضائية سابقة بواسطة محامٍ آخر.
- `دعوى متابعة`: عندما يستلم المكتب دعوى قائمة أو منظورة سابقاً، وقد يكون لها محامٍ سابق أو جلسات أو قرارات أو رقم أساس قائم.

الخطوات المعتمدة:

```text
نوع الإدخال
بيانات ملف المكتب
الموكلون
الخصوم
وكالات الموكلين
نوع الدعوى والاختصاص
المرحلة / المحكمة
تركيب اسم المحكمة
رقم الأساس / المرجع القضائي
موضوع الدعوى والطلبات
الجلسات والموعد القادم
بيانات المحامي السابق ولا مانع في دعوى المتابعة
المستندات الأولية
الأتعاب والمالية
المراجعة والحفظ
```

القواعد المثبتة:

- `رقم ملف المكتب` رقم داخلي وليس رقم الأساس القضائي.
- الموكلون والخصوم متعددون.
- الوكالة يمكن إضافتها الآن أو لاحقاً، والنقص تحذيري لا يمنع الحفظ.
- التصنيفات الرئيسية: مدنية، تجارية، جزائية، أحوال شخصية، قضاء إداري.
- كل خيار ومرحلة يعرض إيضاحاً بسيطاً لمعناه العملي.
- اتفاق الأتعاب لا يساوي سند القبض.
- دعوى المتابعة تطلب بيانات المحامي السابق، رقم هاتفه، لا مانع عند اللزوم، الجلسات والقرارات السابقة، والمستندات المستلمة.
- النواقص تظهر في المراجعة ولا تمنع الحفظ مبدئياً.

تم توثيق التفصيل الكامل في:

```text
docs/NEW_CASE_FOLLOWUP_WIZARD_PLAN.md
```

### قرار تصميمي — ويزارد إدخال أرشيف دعوى قديم 2026-07-18

تم اعتماد ويزارد مستقل لإدخال أرشيف الدعوى القديم من:

```text
ملفات المكتب > إدخال الأرشيف القديم
```

ولا يظهر هذا الويزارد في `مكتب العمل > إنشاء جديد`، ولا في شاشتي `الملفات الجارية` و`الملفات المنتهية` كمدخل إنشاء.

أول سؤال في الأرشيف القديم:

```text
أرشيف جارٍ
أرشيف منتهٍ
```

مع إيضاح أثر كل خيار:

- `أرشيف جارٍ`: ملفات قديمة ما زالت تحت المتابعة، تدخل في الملفات الجارية ومكتب العمل والتقويم عند وجود موعد.
- `أرشيف منتهٍ`: ملفات قديمة انتهى العمل بها، تدخل للبحث والحفظ والطباعة فقط ولا تولد نواقص تشغيلية.

ثم يختار المستخدم نوع الملف، ومسار هذه الخطة هو `دعوى`.

خيارات طريقة إدخال الدعوى الأرشيفية:

```text
إدخال بيانات كاملة
رفع مستندات وتصنيف لاحقاً
إدخال مختصر
```

الخطوات المعتمدة تشمل:

```text
حالة الأرشيف
نوع الملف
طريقة إدخال الدعوى
بيانات ملف المكتب القديم أو رقم أرشيف جديد
كيف دخلت الدعوى إلى المكتب سابقاً
الموكلون والخصوم
الوكالات
تصنيف الدعوى
بيانات المحكمة / المرجع
رقم الأساس / القيد القضائي
حالة المرحلة في الأرشيف
الجلسات السابقة
الأحكام والقرارات السابقة
المستندات الأرشيفية
الأصل الورقي
المالية القديمة
الموعد أو الإجراء القادم
الإجراءات اللاحقة
المراجعة قبل الحفظ
```

القواعد المثبتة:

- الأرشيف الجاري تظهر عليه نواقص تشغيلية ويدخل في مكتب العمل عند وجود موعد.
- الأرشيف المنتهي تظهر عليه نواقص أرشيفية فقط ولا يزعج مكتب العمل.
- رقم ملف المكتب يبقى منفصلاً عن رقم الأساس القضائي.
- يمكن إدخال رقم ملف قديم كما كان في المكتب أو رقم أرشيف جديد مقترح.
- الإجراءات اللاحقة مثل تجديد الدعوى أو الاستئناف أو النقض أو المخاصمة أو التنفيذ تظهر حسب حالة الأرشيف ونوع الدعوى والمرحلة وسبب الانتهاء وصلاحية المستخدم.

تم توثيق التفصيل الكامل في:

```text
docs/OLD_CASE_ARCHIVE_WIZARD_PLAN.md
```

### قرار داعم — قواعد هيكل مكتب المحامي السوري 2026-07-18

بعد مطابقة الهيكل المعتمد مع واقع عمل المحامي السوري وأفضل ممارسات إدارة الملفات القانونية، تم اعتماد قواعد داعمة لا تلغي القرارات السابقة بل تضبطها:

```text
فحص تعارض مصالح / وجود طرف سابق عند إنشاء ملف جديد.
الوكالة ككيان مستقل قابل للربط بعدة ملفات.
دعم العزل والاعتزال كأحداث على الوكالة تؤثر على الملفات الجارية المرتبطة بها.
المستندات تبقى طبقة داخل الملفات، مع بحث وتقارير مركزية لاحقاً، ولا تعود كقسم جانبي مشتت.
محضر تسليم مستندات عند التخلي أو الإغلاق أو تسليم الأصول.
Checklist إغلاق إداري يميز بين انتهاء المرحلة قضائياً وإغلاق الملف مكتبياً بالكامل.
القضاء الإداري يبقى مستقلاً، مع قوائم مرجعية سورية قابلة للتوسع دون تعقيد واجهة المستخدم.
```

أثر القرار على التنفيذ:

- شاشة الملفات الجارية يجب أن تظهر تنبيهات التعارض المحتمل، الوكالات غير المكتملة أو المعزول عنها، والنواقص التشغيلية.
- شاشة الملفات المنتهية يجب أن تظهر حالة الإغلاق الإداري، محاضر التسليم، الأصول الورقية، المالية المتبقية، والإجراءات اللاحقة.
- ويزارد إنشاء ملف جديد يجب أن يدعم فحص وجود طرف سابق قبل الحفظ.
- الوكالات لا تنسخ كبيانات داخل كل ملف فقط، بل تربط ككيان مستقل عند الإمكان.
- ويزارد الإغلاق يجب أن يدعم محضر تسليم المستندات وChecklist الإغلاق الإداري.

تم توثيق التفصيل الكامل في:

```text
docs/SYRIAN_LAW_OFFICE_SUPPORTING_RULES.md
```

### قرار تصميمي — شاشة الملفات الجارية والمنتهية 2026-07-20

تم اعتماد التصميم التفصيلي لشاشة `ملفات المكتب` وفق الفصل التالي:

```text
الملفات الجارية = لوحة تشغيل يومية.
الملفات المنتهية = أرشيف بحث وإجراءات لاحقة.
إدخال الأرشيف القديم = مسار مستقل.
إنشاء الجديد = مكتب العمل فقط.
```

القرارات الأساسية:

- لا إنشاء ملف جديد من شاشة الملفات الجارية أو المنتهية.
- لا إدخال أرشيف قديم كزر إنشاء عشوائي داخل الملفات الجارية أو المنتهية.
- أنواع الملفات تظهر داخل الشاشة كفلاتر: دعاوى، إجراءات / معاملات، عقود، شركات، وكالات.
- الملفات الجارية تعرض مؤشرات تشغيلية مثل المتأخرة، الموعد القريب، تحتاج استكمال، أوامر عمل مفتوحة، تعارض محتمل.
- الملفات المنتهية تعرض مؤشرات أرشيفية وإدارية مثل إغلاق إداري غير مكتمل، إجراءات لاحقة، أصول ورقية في المكتب، مالية متبقية.
- بطاقة الملف الجاري تعرض الموعد القادم، النواقص التشغيلية، أوامر العمل، والأزرار السريعة حسب نوع الملف.
- بطاقة الملف المنتهي تعرض سبب الإغلاق، حالة الإغلاق الإداري، الأصل الورقي، محضر التسليم، المالية، والإجراءات اللاحقة عند وجودها.
- فتح الملف من الجارية يفتح Workspace تشغيلي كامل.
- فتح الملف من المنتهية يفتح نفس Workspace لكن بوضع منتهي / قراءة فقط غالباً، دون نواقص تشغيلية، ومع إجراءات لاحقة / طعون عند الحاجة.
- إعادة فتح ملف منتهٍ تبقى للمالك / المدير فقط وبسبب مسجل.
- المستندات تبقى ضمن الملف مع تقارير وبحث مركزي لاحقاً، ولا تعود كقسم جانبي مستقل مشتت.

تم توثيق التفصيل الكامل في:

```text
docs/OFFICE_FILES_ACTIVE_CLOSED_SCREEN_PLAN.md
```

### قرار ملزم — ترقيم ملفات المكتب وربط الإجراءات بالملفات 2026-07-20

تم اعتماد قاعدة ترقيم ملفات المكتب وربط الإجراءات بالملفات الأخرى بالشكل التالي:

```text
رقم ملف المكتب يولد تلقائياً حسب نوع الملف.
البادئات عربية ومفهومة للمستخدم.
كل نوع ملف له تسلسل مستقل.
رقم ملف المكتب غير قابل للتعديل بعد الحفظ حتى للمدير.
الإجراء / المعاملة ملف مستقل وليس مهمة، ويمكن ربطه بملفات أخرى بعلاقة واضحة دون دمج.
```

صيغة الترقيم المعتمدة:

```text
دعوى/2026/0001
إجراء/2026/0001
عقد/2026/0001
شركة/2026/0001
وكالة/2026/0001
```

قواعد الترقيم:

- يولد الرقم عند الحفظ النهائي فقط.
- لا يكرر الرقم داخل نفس نوع الملف، سواء كان الملف جارياً أو منتهياً أو أرشيفياً أو ملغى.
- لا توجد صلاحية لتعديل الرقم بعد الحفظ.
- إذا حدث خطأ بعد الحفظ، يعالج بإلغاء الملف وفتح ملف صحيح جديد، لا بتعديل الرقم.
- الأرشيف القديم يحتفظ برقمه القديم في حقل مستقل باسم `رقم الملف القديم / الورقي`.
- الروابط الداخلية تعتمد على `id` ثابت، بينما رقم ملف المكتب يبقى للعرض والبحث والطباعة.

قواعد ربط الإجراء:

- الإجراء يبقى ملفاً مستقلاً وله رقم ملف مكتب خاص به.
- يمكن ربط الإجراء بدعوى أو عقد أو شركة أو وكالة أو إجراء آخر.
- الربط لا يعني الدمج.
- عند الربط يحدد المستخدم نوع العلاقة مثل: إجراء ممهد، مستند مؤيد، إجراء مطلوب أثناء الدعوى، إجراء لاحق، الدعوى نشأت بسبب هذا الإجراء.
- عند ربط حصر إرث بدعوى يمكن أن تكون العلاقة: إثبات صفة الورثة، إجراء ممهد، مستند مؤيد، مستند مطلوب من المحكمة، أو سبب فتح الدعوى.
- يمكن إظهار مستندات مختارة من الإجراء داخل الملف المرتبط كمستندات مرتبطة دون نسخ عشوائي.

تم توثيق التفصيل الكامل في:

```text
docs/OFFICE_FILE_NUMBERING_AND_LINKING_RULES.md
```

### قرار تنظيمي — تسلسل المراجعة قبل التنفيذ 2026-07-20

تم اعتماد تسلسل مراجعة الملفات والشاشات قبل الانتقال إلى التنفيذ البرمجي الكبير:

```text
1. ملف الإجراء / المعاملة.
2. ملف العقد.
3. ملف الشركة.
4. ملف الوكالة.
5. مكتب العمل.
6. المالية والصندوق.
7. المستندات والأصل الورقي.
8. القوائم المرجعية النهائية.
9. التقارير والكشوف.
10. المكتبة القانونية والنماذج.
```

مع السماح بالانتقال إلى نقاش ملف العقد الآن بناءً على قرار المستخدم، على أن يبقى ملف الإجراء / المعاملة محفوظاً كمرحلة مراجعة معتمدة لاحقة قبل التنفيذ النهائي إذا احتاج الأمر.

### قرار تصميمي — ملف العقد وويزارد العقود 2026-07-20

تم اعتماد فلسفة ملف العقد داخل التطبيق بالشكل التالي:

```text
ملف العقد في التطبيق هو غلاف وفهرسة ومتابعة، وليس شاشة لإعادة كتابة مضمون العقد.
تفاصيل العقد القانونية والمالية والفنية تبقى داخل نموذج Word / PDF / المستند المرفق.
```

البيانات التي يحفظها التطبيق عن العقد:

```text
رقم ملف المكتب المولد تلقائياً بصيغة عقد/السنة/التسلسل.
نوع ملف العقد: إنشاء عقد جديد، مراجعة / تعديل عقد جاهز، متابعة عقد موقع.
التصنيف القانوني.
التصنيف الفرعي.
أطراف العقد غير المحدودة.
تاريخ العقد.
حالة العقد.
نسخة العقد / النموذج / الملف.
ملاحظات مختصرة للبحث عند الحاجة.
الملفات المرتبطة.
المستندات وأوامر العمل والمالية وسجل الأحداث.
```

لا يطلب التطبيق من المستخدم إعادة إدخال بنود العقد مثل الثمن، شروط التسليم، الغرامات، الضمانات، أو تفاصيل محل العقد كحقول إلزامية داخل الواجهة؛ فهذه التفاصيل تبقى داخل نموذج العقد نفسه.

ويزارد العقد يعتمد التسلسل التالي:

```text
1. نوع ملف العقد.
2. التصنيف القانوني.
3. التصنيف الفرعي.
4. طريقة إنشاء العقد:
   - اختيار نموذج جاهز.
   - إدخال عقد / نموذج للتعديل.
   - كتابة عقد يدوياً.
5. الأطراف.
6. تاريخ العقد وحالته.
7. النسخة / المستندات.
8. المراجعة والحفظ.
```

تم اعتماد نماذج الأستاذ المحامي حازم الجزار كمصدر أولي للنماذج الجاهزة، مع قاعدة ملزمة:

```text
تراجع نماذج الجزار لاحقاً نموذجاً نموذجاً.
يطابق كل نموذج مع القانون السوري والمواد القانونية اللازمة.
يعدل أي نموذج يحتاج إلى تحديث أو تصحيح.
تعرض النسخة النهائية على المحامي للمراجعة والاعتماد قبل إدخالها كقالب نهائي في التطبيق.
```

تم توثيق التفصيل الكامل في:

```text
docs/CONTRACT_FILE_WORKFLOW_PLAN.md
```

### قرار تصميمي — ملف الإجراء / المعاملة 2026-07-20

تم اعتماد فلسفة ملف الإجراء / المعاملة بالشكل التالي:

```text
الإجراء / المعاملة ملف مستقل وليس مهمة.
تفاصيل الإجراء تبقى في المستندات والمرفقات.
التطبيق يحفظ الغلاف والفهرسة والربط والمتابعة فقط.
```

القواعد المعتمدة:

- رقم الإجراء يولد تلقائياً بصيغة `إجراء/السنة/التسلسل`.
- التصنيف يكون رئيسياً ثم فرعياً، مع قوائم سورية واسعة وقابلة للإضافة.
- الجهة تحفظ باسمها وتصنيفها الإداري وتصنيفها المكاني، مثل: `السجل التجاري / مديرية / ريف دمشق`.
- الإجراء يجب دائماً أن يدعم الربط بدعوى قائمة أو إنشاء دعوى يكون هو أساسها.
- خيار إنشاء دعوى مرتبطة من إجراء يجب أن يضاف إلى ويزارد إنشاء الدعوى.
- الإجراء يمكن أن يرتبط بعقد أو شركة أو وكالة أو إجراء آخر دون دمج الملفات.

تم توثيق التفصيل الكامل في:

```text
docs/ADMIN_PROCEDURE_FILE_WORKFLOW_PLAN.md
```

### قرار تصميمي — ملف الشركة وويزارد التأسيس 2026-07-20

تم اعتماد ملف الشركة كملف مستقل يحفظ بيانات التأسيس الأساسية داخل التطبيق، ولا يكتفي بمرفق فقط.

بيانات التأسيس المطلوبة:

```text
تصنيف الشركة بشكل تفصيلي
الشركاء المؤسسون
المفوض بالتأسيس
غاية الشركة
مقر الشركة
اسم الشركة
رأس المال والحصص والإدارة والمدة عند الحاجة
```

تم اعتماد مجموعات الثبوتيات التالية، مع ترك مجال للمستخدم لإدخال اسم الوثيقة وإرفاقها داخل كل مجموعة:

```text
طلب التأسيس
النظام الأساسي / عقد التأسيس
قرار الوزارة بالتأسيس
وثائق المالية
وثائق المقر
وثائق التأمينات الاجتماعية
وثائق غرفة التجارة
وثائق السجل التجاري
إيصالات متفرقة
موافقات خاصة
وثائق أخرى
```

تعديلات ما بعد التأسيس تتم من ملف الشركة الجاري عبر زر `إضافة تعديل`، ولا تحشر في ويزارد التأسيس. أداة التعديل مرنة وتعتمد اختيار الحقل أو الموضوع الأساسي المراد تعديله، ثم اسم التعديل، ثم الوثيقة المرفقة.

تم توثيق التفصيل الكامل في:

```text
docs/COMPANY_FILE_WORKFLOW_PLAN.md
```

### قرار تصميمي — ملف الوكالة 2026-07-20

تم اعتماد ملف الوكالة ككيان مستقل يشمل:

```text
الوكالات القضائية
الوكالات العدلية
الوكالات الخارجية
التفويضات
```

تصنيفات الوكالة القضائية العملية:

```text
سند توكيل عام
سند توكيل خاص
سند توكيل خاص شرعي
```

وتدعم تصديقات / استعمالات:

```text
صلحية
بدائية
جنائية
تنفيذية
```

الوكالة الخارجية يجب أن تدعم:

```text
تصديق وزارة الخارجية
التوطين لدى المحامي العام
إيداع الأصل لدى المحامي العام
استخراج نسخ مصدقة
تعديل الحالة حسب مرحلة التصديق والتوطين
```

العزل والاعتزال يسجلان ببيانات مختصرة:

```text
تاريخ العزل أو الاعتزال
السبب
وثيقة العزل أو الاعتزال
```

ويظهران كنقص / تنبيه تحذيري على الملفات الجارية المرتبطة، دون منع إدخال المعلومات.

تم توثيق التفصيل الكامل في:

```text
docs/AGENCY_FILE_WORKFLOW_PLAN.md
```

### قرار تصميمي — الأدوار الافتراضية وتوزيع عمل الغد 2026-07-20

تم اعتماد تبسيط الأدوار الافتراضية داخل التطبيق لتناسب المحامي الفرد كما تناسب مكاتب وشركات المحاماة الكبيرة.

الأدوار الافتراضية داخل التطبيق:

```text
مالك المكتب
مدير المكتب
محامي أستاذ
```

ولا يتم جعل المحامين المتمرنين أو المعقبين أدوار مستخدمين افتراضية، لأن التطبيق حالياً محلي وليس نظام سيرفر لكل شخص شاشة مستقلة.

بدلاً من ذلك، يدارون كقوائم مرجعية:

```text
قائمة المحامين المتمرنين
قائمة المعقبين / مندوبي المعاملات
```

أثر القرار على مكتب العمل:

- عمل الغد يجب أن يدعم `المسؤول / المشرف` و`المنفذ / المكلف`.
- المسؤول يكون غالباً مستخدماً داخل التطبيق: مالك المكتب، مدير المكتب، محامي أستاذ.
- المنفذ يمكن أن يكون محامياً متمرناً أو معقباً من القوائم المرجعية.
- يمكن طباعة أو إرسال مهام الغد حسب المحامي الأستاذ أو المحامي المتمرن أو المعقب أو الجهة.
- إدخال النتيجة يتم من مستخدم فعلي، مع حفظ اسم المنفذ الفعلي ومدخل النتيجة.

تم توثيق التفصيل الكامل في:

```text
docs/DEFAULT_ROLES_AND_WORK_ASSIGNMENT_PLAN.md
```

### قرار تصميمي — شاشة عمل الغد وإرسال المهام 2026-07-20

تم اعتماد أن `عمل الغد` هو شاشة تحضير وتوزيع لما يجب تنفيذه غداً، وليس شاشة إدخال نتائج.

القواعد المعتمدة:

```text
عمل الغد = كل عنصر تاريخ عمله غداً، سواء أضيف اليوم أو رُحّل من الماضي أو نتج عن ملف قديم.
عمل اليوم = شاشة تحصيل النتائج ومعالجتها.
```

شاشة عمل الغد تعتمد شاشة واحدة مع طرق عرض وتجميع:

```text
حسب المنفذ / المكلف
حسب المحامي الأستاذ المشرف
حسب الجهة
حسب نوع العمل
كل المهام
```

وتظهر المهام كبطاقات عصرية تحتوي:

```text
نوع العمل
رقم ملف المكتب
عنوان الملف
الموكل أو صاحب العلاقة
الجهة / المحكمة / الدائرة
الموعد / الوقت
المطلوب تنفيذه
المستندات المطلوبة
المسؤول / المشرف
المنفذ / المكلف
ملاحظات الإرسال / الطباعة
```

كما تم اعتماد دعم:

```text
طباعة عمل الغد كاملاً أو حسب المنفذ أو المشرف أو الجهة.
إرسال مهام الغد عبر واتس اب المكتب.
نسخ نص الرسالة كحل مضمون.
إصلاح فتح واتس اب بحيث يمرر النص الجاهز للرسالة عند توفر رقم الهاتف.
```

تم توثيق التفصيل الكامل في:

```text
docs/TOMORROW_WORK_SCREEN_PLAN.md
```

### قرار تصميمي — شاشة أعمال اليوم والمهام غير المكتملة 2026-07-20

تم اعتماد أن `أعمال اليوم` هي شاشة تحصيل نتائج ومعالجة، وتعرض الأعمال حسب تقسيم التطبيق:

```text
دعاوى
إجراءات / معاملات
عقود
شركات
وكالات
مهام عامة عند الحاجة
```

ولا يظهر في الشاشة إلا القسم الذي يحتوي أعمالاً فعلية في ذلك اليوم.

القواعد المعتمدة:

- قسم الدعاوى يعرض بمنطق أجندة المحامي: المحكمة، الغرفة، رقم الأساس، الموكل، الخصم، موضوع الدعوى، الإجراء المطلوب، المنفذ، وإدخال النتيجة.
- قسم الإجراءات يعرض: الجهة، التصنيف الإداري والمكاني، رقم المعاملة، صاحب العلاقة، نوع الإجراء، المطلوب، المنفذ، وإدخال النتيجة.
- العقود والشركات والوكالات تعرض حسب منطق كل نوع ملف.
- في نهاية كل مهمة يوجد خيار `إدخال النتيجة`، وتختلف خيارات النتيجة حسب نوع العمل.
- بعد حفظ النتيجة، يفرز التطبيق آثارها إلى الملف المرتبط، المستندات، المالية، المواعيد، النواقص، وأحداث الملف.
- عند تغير التاريخ، كل أعمال اليوم التي لم تدخل نتيجتها تنتقل تلقائياً إلى `مهام غير مكتملة`.
- المهام غير المكتملة لا ترحل تلقائياً إلى اليوم الجديد أو الغد، بل تعالج يدوياً بإدخال نتيجة متأخرة أو ترحيل أو تأجيل أو إلغاء بسبب.

تم أيضاً تثبيت أن التصميم الشكلي النهائي للواجهات يؤجل إلى ما بعد استكمال البرمجة والهيكل الوظيفي الأساسي، لأن الأولوية الآن لضبط المنطق والبيانات.

تم توثيق التفصيل الكامل في:

```text
docs/TODAY_WORK_SCREEN_PLAN.md
```

### قرار تشغيلي — توزيع عمل الغد 2026-07-20

تم اعتماد أن شاشة `عمل الغد` ليست شاشة عرض فقط، بل تدعم عملية تشغيلية باسم:

```text
تحضير / توزيع عمل الغد
```

القواعد:

- عمل الغد يجمع تلقائياً من كل الملفات والأوامر التي تاريخها الغد.
- توزيع عمل الغد يتم غالباً من شاشة اليوم عبر زر `تحضير / توزيع عمل الغد`، ويمكن تعديله من شاشة عمل الغد نفسها.
- كل مهمة غد تحتوي خانة `إسناد إلى` لاختيار المنفذ من المحامين المتمرنين أو المعقبين أو مستخدم داخلي.
- يدعم النظام فلتر `غير مسند` وإسناداً جماعياً لعدة مهام.
- بعد الإسناد يمكن عرض المهام حسب المنفذ وطباعة كشف عام أو كشف لكل شخص أو بطاقات منفصلة.
- يمكن إرسال مهام كل منفذ عبر واتس اب المكتب، مع نسخ النص كحل احتياطي.
- تحفظ حالة توزيع المهمة: غير مسندة، مسندة، مطبوعة، مرسلة واتس اب، معدلة بعد الإرسال.
- اعتماد توزيع عمل الغد اختياري، لكنه يسجل من اعتمد التوزيع وأي تعديل لاحق عليه.

تم توثيق التفصيل ضمن:

```text
docs/TOMORROW_WORK_SCREEN_PLAN.md
```

### قرار تصميمي — أوامر العمل ودورة حياتها 2026-07-20

تم اعتماد أن أمر العمل هو مهمة تنفيذية داخل مكتب العمل أو داخل ملف، وليس ملفاً مستقلاً.

القاعدة الأساسية:

```text
الإجراء / المعاملة = ملف مستقل.
أمر العمل = مهمة.
```

حالات أمر العمل المعتمدة:

```text
مفتوح
نتيجة مدخلة
معتمد
مؤجل
ملغى
يحتاج تصحيح
```

أنواع أوامر العمل الافتراضية:

```text
حضور جلسة
مراجعة جهة
دفع رسم
استلام وثيقة
تسليم مستند
إحضار مستند
تحضير مذكرة
طباعة / تصوير
مراجعة موكل
اتصال هاتفي
إرسال رسالة
توقيع
متابعة مالية
متابعة وكالة
متابعة عقد
متابعة شركة
عمل أرشيفي
مهمة عامة
```

قرارات إضافية:

- الاعتماد يكون حسب صلاحية المستخدم.
- مالك المكتب ومحامي أستاذ يستطيعان الاعتماد، ومدير المكتب إذا منحه المالك صلاحية اعتماد.
- أمر العمل العام مسموح ولو لم يرتبط بملف.
- أمر العمل يعتمد منفذاً رئيسياً واحداً في المرحلة الحالية.
- نتيجة أمر العمل يمكن أن تنشئ أثراً مالياً إذا توفرت الصلاحية أو بعد الاعتماد.
- أمر العمل يظهر في عمل الغد أو اليوم حسب تاريخ العمل.
- أمر العمل الذي يتجاوز تاريخه دون نتيجة ينتقل إلى المهام غير المكتملة.
- لا يحذف أمر العمل عند الإلغاء، بل تحفظ حالته وسجل أحداثه.

تم توثيق التفصيل الكامل في:

```text
docs/WORK_ORDER_LIFECYCLE_PLAN.md
```

### قرار تصميمي — تقويم مكتب العمل 2026-07-20

تم اعتماد أن شاشة التقويم في مكتب العمل هي مفكرة عمل حسب التاريخ، وليست تقويماً عاماً فارغاً.

التخطيط المعتمد:

```text
يمين الشاشة: تقويم صغير لأيام الشهر.
وسط / يسار الشاشة: أعمال اليوم المختار.
```

القواعد:

- الضغط على أي يوم يعرض الأعمال الموجودة في ذلك التاريخ.
- لا تظهر الأقسام الفارغة؛ تظهر فقط الأقسام التي تحتوي أعمالاً: دعاوى، إجراءات، عقود، شركات، وكالات، مهام عامة.
- أيام الشهر التي تحتوي أعمالاً تظهر عليها مؤشرات أو عدد أعمال.
- اليوم الحالي يسمح بإدخال النتائج أو فتح شاشة أعمال اليوم.
- الغد يسمح بالتحضير والتوزيع والطباعة والإرسال.
- التاريخ المستقبلي يسمح بالعرض والتخطيط والإسناد.
- التاريخ الماضي يسمح بمراجعة النتائج وإدخال نتيجة متأخرة أو عرض المهام غير المكتملة.
- يمكن إضافة عمل من التقويم كأمر عمل أو موعد مرتبط بملف، لكن لا يجوز إنشاء ملف جديد من التقويم.

تم توثيق التفصيل الكامل في:

```text
docs/WORK_CALENDAR_SCREEN_PLAN.md
```

### قرار تصميمي — شاشة إنشاء جديد في مكتب العمل 2026-07-20

تم اعتماد أن `إنشاء جديد` هو البوابة الوحيدة للعمل الجديد بعد نزول التطبيق، ويوجد فقط داخل:

```text
مكتب العمل
```

ولا يظهر داخل:

```text
الملفات الجارية
الملفات المنتهية
التقويم
إدخال الأرشيف القديم
```

الخيارات الأساسية في شاشة إنشاء جديد:

```text
دعوى
إجراء / معاملة
عقد
شركة
وكالة
أمر عمل عام
```

قواعد الشاشة:

- لا يظهر إدخال الأرشيف القديم داخل إنشاء جديد.
- لا يظهر إنشاء شخص / جهة مستقلة أو مستند مستقل.
- يظهر بحث قبل الإنشاء لفحص وجود ملفات أو أطراف سابقة وتجنب التكرار.
- كل ويزارد يعرض أن رقم ملف المكتب سيولد تلقائياً عند الحفظ.
- بعد الحفظ تظهر خيارات: فتح الملف، إضافة أمر عمل، إضافة مستند، العودة إلى مكتب العمل.
- بطاقات الإنشاء تظهر حسب صلاحيات المستخدم.
- أي موعد يدخل أثناء الإنشاء يظهر في أعمال اليوم أو عمل الغد أو التقويم حسب التاريخ.

تم توثيق التفصيل الكامل في:

```text
docs/NEW_WORK_CREATION_SCREEN_PLAN.md
```

### قرار تصميمي — المالية والصندوق 2026-07-20

تم اعتماد إعادة هيكلة `المالية والصندوق` وفق تجربة مرنة تناسب واقع المحامي السوري، مع قواعد ثابتة وقوائم قابلة للتخصيص.

القواعد الثابتة:

```text
اتفاق أتعاب ≠ سند قبض.
مبالغ لحساب الموكل ≠ أتعاب المكتب.
المصروف قد يخرج من صندوق المكتب وقد لا يخرج منه.
لا حذف مالي نهائي.
كل حركة مالية لها سجل وأثر تدقيق.
القوائم المالية قابلة للتخصيص حسب سياسة المكتب.
```

التبويبات المعتمدة:

```text
لوحة مالية
اتفاقيات الأتعاب
سندات القبض
المصاريف
مبالغ لحساب الموكل
ذمم الموكلين
صندوق المكتب
التقارير المالية
الإعدادات المالية
```

قرارات مهمة:

- التسمية المعتمدة لتبويب أموال الموكلين هي `مبالغ لحساب الموكل`.
- سندات القبض ترقم بصيغة `قبض/2026/0001`.
- المصاريف ترقم بصيغة `مصروف/2026/0001`.
- مبالغ لحساب الموكل ترقم بصيغة `مبلغ-موكل/2026/0001`.
- يبدأ النظام بصندوق رئيسي، مع إمكانية إضافة صناديق أو حسابات من الإعدادات المالية.
- الإعدادات المالية تظهر كزر / تبويب داخل المالية وتفتح القوائم المرجعية المالية.
- اتفاق الأتعاب كيان مستقل مرتبط بالملف، ويدعم المقطوع والنسبة وحسب الجلسة وحسب المرحلة والمختلط والمجاني.
- عند الاتفاق بالنسبة يظهر تنبيه للتحقق من توافق النسبة مع قانون تنظيم مهنة المحاماة.
- سند القبض يسأل عن نوع المقبوض حتى لا يخلط بين الأتعاب، استرداد المصروف، والمبالغ لحساب الموكل.
- المصروف يسأل: من دفع؟ من يتحمل؟ هل خرج من صندوق المكتب؟ هل قابل للاسترداد؟
- كل ملف يحتوي تبويب مالية خاص به.

تم توثيق التفصيل الكامل في:

```text
docs/FINANCE_CASHBOX_WORKFLOW_PLAN.md
```

### قرار تصميمي — المستندات والأصل الورقي 2026-07-20

تم اعتماد أن المستندات والأصل الورقي لا تكون بنداً رئيسياً مستقلاً في الشريط الجانبي.

القواعد:

```text
المستندات طبقة داخل كل ملف وتبويب بحسب السياق.
الأصل الورقي خاصية للمستند أو الملف.
محضر تسليم المستندات يظهر ضمن الإغلاق والتسليم.
البحث والكشوف المركزية للمستندات تكون ضمن التقارير والكشوف أو البحث العام.
```

أماكن ظهور المستندات:

```text
الدعوى: تبويب المستندات.
الإجراء / المعاملة: تبويب المستندات.
العقد: نسخة العقد / المستندات.
الشركة: الثبوتيات والمستندات.
الوكالة: بيانات السند والتصديقات.
المالية: مرفقات الحركة المالية.
الأرشيف القديم: رفع وتصنيف وربط المستندات.
```

كما تم اعتماد:

- حالات للأصل الورقي مثل: موجود في المكتب، مسلم للموكل، مسلم لمحامٍ آخر، مسلم لجهة رسمية، لدى الكاتب بالعدل / الجهة، مفقود، غير معروف، لا يوجد أصل.
- مكان أصل ورقي: الموقع، الخزانة، الرف، الصندوق، المصنف، رقم الغلاف.
- منع التكرار عبر بصمة رقمية مع السماح بالربط بالمستند السابق أو رفع نسخة جديدة.
- قائمة مرجعية قابلة للتخصيص لأنواع المستندات.

تم توثيق التفصيل الكامل في:

```text
docs/DOCUMENTS_AND_PAPER_ORIGINALS_PLAN.md
```

### قرار تصميمي — القوائم المرجعية النهائية 2026-07-20

تم اعتماد خطة القوائم المرجعية النهائية بحيث تكون القوائم الافتراضية واسعة وشاملة قدر الإمكان لواقع المحامي السوري، مع إبقاء إمكانية إضافة عناصر وقوائم مخصصة.

المجموعات المعتمدة:

```text
عام
دعاوى ومحاكم
إجراءات وجهات
عقود ونماذج
شركات
وكالات ونقابة
مكتب العمل
توزيع العمل
مالية
مستندات وأرشيف
إغلاق وتسليم
تقارير
مكتبة قانونية
قوائم مخصصة
```

القواعد:

- إدارة القوائم تكون من `إدارة المكتب > القوائم المرجعية`.
- الإضافة السريعة من الويزاردات اختصار فقط، وليست مكان الإدارة الرئيسي.
- كل قائمة تدعم الإضافة، التعديل، التعطيل، إعادة التفعيل، الحذف الآمن إذا غير مستخدمة، البحث، الترتيب، والملاحظات.
- كل عنصر يميز بين `نظامي` و`مخصص`.
- لا يحذف أي عنصر مستخدم سابقاً، بل يعطل فقط.
- تدعم الخطة إضافة قائمة مرجعية مخصصة بسيطة أو هرمية.
- تؤجل الحقول المخصصة المرتبطة بالقوائم إلى مرحلة لاحقة.

تشمل الخطة قوائم موسعة لكل محاور التطبيق: المحاكم والدعاوى، الإجراءات والجهات، العقود والنماذج، الشركات، الوكالات والنقابة، مكتب العمل، التوزيع، المالية، المستندات والأرشيف، الإغلاق والتسليم، التقارير، والمكتبة القانونية.

تم توثيق التفصيل الكامل في:

```text
docs/FINAL_REFERENCE_LOOKUPS_PLAN.md
```

### قرار تصميمي — التقارير والكشوف 2026-07-20

تم اعتماد قسم `التقارير والكشوف` كمركز استخراج وطباعة وتصدير، وليس مركز تشغيل يومي.

طريقة العرض:

```text
شاشة واحدة مقسمة إلى مجموعات.
قسم التقارير المفضلة في أعلى الشاشة.
```

مجموعات التقارير:

```text
كشوف العمل اليومي
كشوف الملفات
كشوف الدعاوى والجلسات
كشوف الإجراءات والمعاملات
كشوف العقود والشركات والوكالات
كشوف المالية والصندوق
كشوف المستندات والأصل الورقي
كشوف النواقص
كشوف الأرشيف القديم
كشوف الإدارة والتدقيق
```

القواعد:

- كل تقرير يدعم الترويسة الرسمية للمكتب.
- كل تقرير يدعم الفلاتر المناسبة له.
- يدعم PDF و CSV من البداية قدر الإمكان.
- Excel يؤجل إلى مرحلة لاحقة.
- الطباعة المركزية تكون من التقارير والكشوف، أما طباعة ملف محدد فتبقى داخل الملف نفسه.
- حفظ إعدادات التقرير كقالب محفوظ يؤجل إلى مرحلة ثانية.

تم توثيق التفصيل الكامل في:

```text
docs/REPORTS_AND_EXPORTS_PLAN.md
```

### قرار تصميمي — استخدام النماذج القانونية 2026-07-20

تم اعتماد خطة مبسطة للنماذج القانونية.

القواعد:

```text
النماذج القانونية هي قوالب لإنشاء مستندات داخل الملفات.
النموذج الأصلي لا يعدل عند الاستخدام.
عند اختيار نموذج، ينشئ التطبيق نسخة جديدة مرتبطة بالملف.
```

مسارات النماذج المعتمدة:

```text
النماذج الجاهزة: نماذج حازم الجزار بعد مراجعتها وتجهيزها قبل البرمجة.
استيراد نموذج: المستخدم يدخل نماذجه الخاصة.
كتابة مستند جديد: فتح محرر بسيط أو ملف Word فارغ حسب الإمكانية التقنية.
```

الاستخدام اليومي للنماذج يكون من داخل الملفات:

```text
الدعوى: استدعاء، مذكرة، طلب، إنذار.
العقد: اختيار نموذج عقد حسب التصنيف.
الإجراء: طلبات ومعاملات.
الشركة: طلب تأسيس، عقد تأسيس، نظام أساسي، قرارات.
الوكالة: عزل، اعتزال، طلب صورة، توطين.
المالية: اتفاق أتعاب، سند قبض، كشف مالي.
الإغلاق: لا مانع، محضر تسليم، مخالصة.
```

قسم `النماذج القانونية` يبقى للإدارة المركزية: استعراض النماذج الجاهزة، استيراد نموذج، إدارة نماذج المكتب، وكتابة مستند مستقل عند الحاجة.

تم توثيق التفصيل الكامل في:

```text
docs/LEGAL_TEMPLATES_USAGE_PLAN.md
```

### قرار تصميمي — المكتبة القانونية 2026-07-20

تم اعتماد أن المكتبة القانونية قسم مرجعي مستقل داخل التطبيق.

الأقسام الرئيسية عند فتح المكتبة:

```text
قوانين ومراسيم تشريعية
اجتهادات محكمة النقض
مجلة المحامون
أبحاث قانونية
تعاميم وقرارات إدارية
كتب ومراجع
```

القواعد:

- القوانين والمراسيم التشريعية تظهر في تبويب واحد.
- تعرض القوانين والمراسيم بفهرس أبجدي وبترتيب الأحدث إلى الأقدم مع فلترة حسب النوع والتصنيف والسنة.
- اجتهادات محكمة النقض تقسم حسب الغرفة: الهيئة العامة، المدنية، الجزائية، التجارية، الأحوال الشخصية / الشرعية، المخاصمة، وغيرها.
- مجلة المحامون تفهرس حسب السنة والعدد، وتفهرس محتوياتها داخلياً في مرحلة لاحقة.
- الأبحاث القانونية تفهرس حسب الباحث والموضوع والتاريخ.
- التعاميم والقرارات الإدارية تركز افتراضياً على ما صدر منذ 2024/12/08 وما بعد.
- كل مرجع يدعم ملفاً أو رابطاً أو نصاً، ويمكن ربطه بملف قانوني عند الحاجة.

تم توثيق التفصيل الكامل في:

```text
docs/LEGAL_LIBRARY_PLAN.md
```

### قرار تنظيمي — جمع وتحميل المحتوى القانوني قبل التنفيذ 2026-07-20

قبل إعداد خطة التنفيذ البرمجية النهائية، يجب الانتهاء من أو تحديد خطة واضحة لجمع وتحميل وفهرسة المحتوى الذي يحتاجه التطبيق.

المحتوى المطلوب:

```text
نماذج حازم الجزار
القوانين والمراسيم التشريعية السورية
اجتهادات محكمة النقض والهيئة العامة
مجلة المحامون
التعاميم والقرارات الإدارية من تاريخ 2024/12/08 وما بعد
الأبحاث القانونية السورية المهمة
كتب ومراجع قانونية مختارة
```

كل ملف محتوى يجب أن يحمل:

```text
العنوان
المصدر
رابط المصدر
نوع المحتوى
حالة التحميل
حالة الفهرسة
حالة التحقق
ملاحظات المراجعة
```

تم توثيق التفصيل الكامل في:

```text
docs/LEGAL_CONTENT_COLLECTION_TASKS.md
```

### قرار تصميمي — الصلاحيات النهائية وهرمية الأدوار 2026-07-20

تم اعتماد هرم صلاحيات بسيط وقوي:

```text
مالك المكتب
مدير المكتب
محامي أستاذ
```

القواعد الأساسية:

- مالك المكتب يملك كل الصلاحيات وصلاحيات السيادة.
- مدير المكتب مسؤول تشغيلي ولا يستطيع تعيين مالك أو مدير مكتب.
- مدير المكتب يستطيع تعيين وإدارة ما دون المدير فقط.
- محامي أستاذ هو المستخدم القانوني الأساسي ولا يملك صلاحيات إدارية أو مالية حساسة افتراضياً.
- الصلاحيات المالية الحساسة، ومبالغ لحساب الموكل، والصندوق، والإلغاء المالي تبقى للمالك افتراضياً.
- لا يسمح لأي مستخدم بمنح صلاحيات لا يملكها.
- لا يسمح لأي مستخدم بتعديل دور أعلى أو مساوٍ له.
- لا يجوز حذف أو تعطيل آخر مالك مكتب.

تم توثيق التفصيل الكامل في:

```text
docs/FINAL_PERMISSIONS_AND_ROLE_HIERARCHY_PLAN.md
```

### قرار تجاري — الترخيص والتفعيل وحماية النسخ 2026-07-20

تم اعتماد خطة ترخيص تجاري مرحلية لحماية التطبيق عند بيعه.

المرحلة الأولى:

```text
تفعيل Offline بملف ترخيص موقّع رقمياً.
ملف طلب تفعيل يصدر من جهاز العميل أو سيرفر الشبكة.
أداة توليد تراخيص لدى البائع.
ربط الترخيص ببصمة الجهاز أو السيرفر.
```

المرحلة الثانية لاحقاً:

```text
تحقق Online دوري من سيرفر تفعيل خارجي عند توفره.
كشف النسخ المنسوخة أو التراخيص المشبوهة.
إلغاء أو نقل أو تجديد التراخيص عن بعد.
فترة سماح Offline ووضع قراءة فقط عند فشل التحقق بعد انتهاء المهلة.
```

أنواع التراخيص:

```text
نسخة فردية — مرتبطة بجهاز واحد.
نسخة شبكة — مرتبطة بسيرفر داخلي وعدد مستخدمين.
```

تم توثيق التفصيل الكامل في:

```text
docs/COMMERCIAL_LICENSING_AND_ACTIVATION_PLAN.md
```

### قرار تقني — التشغيل على شبكة داخلية 2026-07-20

تم اعتماد أن نسخة الشبكة الداخلية لا تستخدم SQLite عبر مجلد شبكة مشترك.

القواعد:

```text
النسخة الفردية: SQLite محلي.
النسخة الشبكية: Local Server + PostgreSQL + مرفقات مركزية.
أجهزة المستخدمين تتصل بالسيرفر المحلي عبر LAN.
الترخيص الشبكي يرتبط بالسيرفر الداخلي وعدد المستخدمين.
المرفقات ترفع وتدار عبر السيرفر المحلي.
النسخ الاحتياطي والاستعادة مركزية وحساسة.
```

الهدف أن يعمل التطبيق من محامٍ فردي إلى شركة محاماة كبيرة، مع قابلية التوسع لاحقاً.

تم توثيق التفصيل الكامل في:

```text
docs/INTERNAL_NETWORK_DEPLOYMENT_PLAN.md
```

