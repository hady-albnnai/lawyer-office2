# خطة إعادة هيكلة مكتب العمل اليومي في ميزان

> **اسم المرحلة:** مكتب العمل اليومي — Daily Work Center  
> **النطاق:** لوحة اليوم، لوحة الغد، الأسبوع، التقويم، إدخال النتائج، توزيع العمل، طباعة لوائح العمل، المهام المخصصة، وربط كل ذلك بالصلاحيات وسجل المسؤولية.  
> **الحالة:** خطة هيكلية معتمدة تُنفّذ بعد إغلاق طبقة الصلاحيات والتدقيق، وتُستخدم قبل بناء المساعد الذكي.

---

## 1. الفكرة الأساسية

الاسم الحالي مثل:

```text
لوحة اليوم
```

لا يكفي لوصف طريقة عمل المحامي. المطلوب هو:

```text
مكتب العمل
```

أو:

```text
مكتب العمل اليومي
```

لأنه ليس مجرد Dashboard، بل مركز تشغيل يومي للمكتب.

هذا المركز يجيب عن أسئلة المحامي العملية:

```text
ماذا حدث اليوم ويحتاج نتيجة؟
ماذا عندي غداً لأحضّره وأوزعه؟
ما الأعمال القادمة خلال الأسبوع؟
من المكلّف بكل عمل؟
ما الملفات التي تحتاج انتباه؟
ما الذي يجب طباعته وتوزيعه على الفريق؟
```

---

## 2. التسمية المعتمدة داخل التطبيق

### الاسم الرئيسي

```text
مكتب العمل
```

### التبويبات الرئيسية

```text
اليوم
الغد
الأسبوع
التقويم
```

### الهدف من كل تبويب

```text
اليوم: إدخال النتائج والمتابعة
الغد: التحضير وتوزيع العمل
الأسبوع: رؤية الأعمال القادمة
التقويم: عرض زمني تفاعلي للأعمال
```

---

## 3. فلسفة اليوم والغد

### 3.1 اليوم

تبويب اليوم ليس فقط لعرض المواعيد، بل هدفه الأساسي:

```text
إدخال نتائج أعمال اليوم
```

أمثلة:

```text
جلسة اليوم → ماذا حدث؟ ما الموعد القادم؟ ما المطلوب؟
إجراء اليوم → هل تم؟ ما الخطوة التالية؟
أمر عمل اليوم → هل عاد المعقب بنتيجة؟
نقص اليوم → هل تم استكماله؟
مهمة مخصصة اليوم → هل أُنجزت أو تُرحّل؟
```

### 3.2 الغد

تبويب الغد هو الأهم للتحضير:

```text
تحضير وتوزيع عمل الغد
```

المحامي غالباً لا يخطط لليوم فقط، بل يحضر للغد:

```text
تحضير ملف جلسة الغد
توزيع مراجعة الدوائر
إرسال المعقب
تجهيز مذكرة
تأكيد حضور الموكل
مراجعة النواقص قبل الجلسة
```

لذلك يجب أن يكون تبويب الغد واضحاً وقابلاً للطباعة والتوزيع.

---

## 4. قاعدة العرض الأساسية

أي شيء تاريخه هو تاريخ التبويب المعروض يجب أن يظهر إذا كان المستخدم يملك صلاحية رؤيته.

مثلاً في تبويب اليوم:

```text
كل عمل تاريخه اليوم يظهر
```

وفي تبويب الغد:

```text
كل عمل تاريخه غداً يظهر
```

لكن مع احترام الصلاحيات:

```text
المالية لا تظهر لمن لا يملك finance.view
أوامر العمل لا تظهر لمن لا يملك work_orders.view
المستندات لا تفتح لمن لا يملك documents.open
سجل المسؤولية لا يظهر لمن لا يملك audit.view
```

---

## 5. أنواع عناصر العمل اليومية

يجب توحيد كل ما يظهر في مكتب العمل تحت مفهوم:

```text
عنصر عمل
```

أو تقنياً:

```text
DailyWorkItem
```

أنواعه:

```text
جلسة
تحضير لجلسة
مراجعة إجراء إداري
أمر عمل
نقص
تذكير عقد
تذكير مالي
مرحلة شركة
مهمة مخصصة
ملف مستورد يحتاج استكمال
مراجعة مستند
```

---

## 6. نموذج عنصر العمل الموحد

كل عنصر عمل يجب أن يحتوي على الأقل:

```text
id
sourceType
sourceId
workType
title
subtitle
entityType
entityId
entityTitle
clientName
dueDate
dueTime
priority
status
assignedToUserId
assignedToName
assignedToType
requiresResult
requiresApproval
canPostpone
canComplete
permissionsRequired
```

### assignedToType

لأن المحامي ليس مضطراً لإنشاء حساب لكل موظف أو معقب، يجب دعم:

```text
internal_user
external_name
unassigned
```

أمثلة:

```text
المحامي سامر — مستخدم داخل النظام
المعقب أبو علي — اسم حر
السكرتيرة — اسم حر
غير مكلف
```

---

## 7. حالات عنصر العمل

نلتزم بدورة حياة واضحة:

```text
مجدول
قيد التنفيذ
تم
مؤجل
ملغى
بانتظار اعتماد
متعذر
```

وتقنياً:

```text
scheduled
in_progress
completed
postponed
cancelled
waiting_approval
impossible
```

---

## 8. ترتيب العرض داخل مكتب العمل

لا تعرض العناصر كقائمة عشوائية. الترتيب المقترح:

```text
1. يحتاج انتباه
2. جلسات
3. تحضيرات ومراجعات
4. أوامر العمل
5. النواقص
6. مهام مخصصة
7. تذكيرات العقود والمالية
8. ملفات مستوردة تحتاج استكمال
```

القسم لا يظهر إذا:

```text
لا يوجد عناصر فيه
أو المستخدم لا يملك صلاحية رؤيته
```

---

## 9. قسم يحتاج انتباه

قسم مهم يظهر في أعلى مكتب العمل.

يعرض:

```text
جلسة اليوم بلا وكالة
جلسة اليوم بلا مستند مطلوب
أمر عمل متأخر
أمر عمل بانتظار اعتماد
دعوى جارية مستوردة بلا جلسة قادمة
إجراء جارٍ بلا خطوة تالية
نقص حرج
تذكير مالي مهم إن كان المستخدم يملك صلاحية المالية
```

---

## 10. بطاقة عنصر العمل

كل بطاقة يجب أن تكون مفهومة للمحامي.

### مثال بطاقة جلسة

```text
جلسة — عالية
دعوى تعويض أحمد الخطيب
محكمة البداية المدنية — أساس 123/2024
الوقت: 09:00
المكلف: المحامي سامر
المطلوب: تقديم مذكرة

[فتح الملف] [إدخال نتيجة] [المزيد]
```

### مثال بطاقة أمر عمل

```text
أمر عمل — تصوير ضبط
الملف: دعوى أحمد الخطيب
المكلف: المعقب أبو علي
موعد التنفيذ: غداً
الحالة: بانتظار نتيجة

[فتح الأمر] [إرسال واتساب] [نتيجة]
```

---

## 11. إدخال النتيجة من تبويب اليوم

في تبويب اليوم، كل عنصر مستحق اليوم يجب أن يملك زر:

```text
إدخال نتيجة
```

لكن نافذة النتيجة تختلف حسب نوع العنصر.

---

## 12. نتيجة الجلسة

نافذة نتيجة الجلسة يجب أن تسأل:

```text
ماذا حدث في الجلسة؟
النتيجة
الموعد القادم
ما المطلوب قبل الموعد القادم؟
هل تريد تذكيراً قبل الموعد؟
هل تحتاج أمر عمل؟
هل توجد مرفقات؟
```

### النتائج المقترحة

```text
تأجيل
حجز للحكم
صدور قرار
شطب
متابعة
إبراز مذكرة
إبراز وثيقة
أخرى
```

### الترحيل التلقائي

عند إدخال الموعد القادم:

```text
ينشأ عنصر عمل جديد بتاريخ الموعد القادم
```

إذا أضاف المستخدم تذكيراً تحضيرياً:

```text
ينشأ عنصر تحضير بتاريخ قبل الموعد القادم
```

مثال:

```text
الجلسة القادمة: 2026-08-20
تذكير تحضيري قبل 5 أيام
→ مهمة تحضير بتاريخ 2026-08-15
```

---

## 13. نتيجة الإجراء الإداري

تسأل:

```text
ماذا تم؟
النتيجة
الخطوة التالية
موعد المراجعة القادم
المكلف التالي
النواقص
المرفقات
```

النتائج:

```text
تم الإنجاز
يحتاج مراجعة أخرى
نقص مستند
تعذر
مؤجل
```

---

## 14. نتيجة أمر العمل

تعتمد على ما تم تخطيطه سابقاً:

```text
نتيجة التنفيذ
تفاصيل النتيجة
موعد لاحق
مرفقات وصور
اعتماد لاحق إن لزم
```

وتربط المرفقات بالملف المرتبط وأمر العمل.

---

## 15. نتيجة النقص

تسأل:

```text
هل تم استكمال النقص؟
ما المستند أو الإجراء الذي تم؟
هل توجد مرفقات؟
ملاحظة
```

إذا تم:

```text
يغلق النقص
يربط المستند
يسجل Timeline و Audit
```

---

## 16. المهام المخصصة

### 16.1 لماذا نحتاجها؟

مهام المحامي لا يمكن حصرها بكل الأنواع النظامية.

أمثلة:

```text
اتصال بموكل
مقابلة شاهد
تحضير مذكرة
مراجعة ملف
متابعة خبير
اجتماع داخلي
تدقيق مستندات
مراجعة بريد
زيارة محكمة غير مرتبطة بملف محدد
```

لذلك نضيف:

```text
مهمة مخصصة
```

---

## 17. قوالب المهام المخصصة

لا تكون المهمة المخصصة مجرد نص حر فقط. الأفضل دعم:

```text
قوالب مهام
```

مثال قالب:

```text
اتصال بموكل
```

نتائجه:

```text
تم الاتصال
لم يرد
طلب موعد
أرسل مستندات
يحتاج متابعة لاحقة
```

مثال آخر:

```text
تحضير مذكرة
```

نتائجه:

```text
تم التحضير
تحتاج مراجعة الأستاذ
تحتاج مستندات
مؤجلة
```

---

## 18. نتائج المهمة المخصصة وآثارها

كل نتيجة يمكن أن يكون لها أثر تلقائي.

### أنواع الأثر

```text
إغلاق المهمة
ترحيل المهمة
إنشاء متابعة
إنشاء نقص
إنشاء أمر عمل
تحتاج اعتماد
لا إجراء
```

تقنياً:

```text
complete
postpone
create_followup_task
create_deficiency
create_work_order
needs_approval
no_action
```

---

## 19. بنية قوالب المهام

### task_templates

```text
id
name
description
defaultPriority
defaultDuration
isActive
createdBy
createdAt
```

### task_template_outcomes

```text
id
templateId
label
resultCode
effectType
requiresDate
requiresNote
requiresAttachment
createsDeficiency
createsWorkOrder
completesTask
postponesTask
sortOrder
```

---

## 20. إضافة عمل لليوم

في مكتب العمل يوجد زر:

```text
إضافة عمل
```

لا يفتح نموذجاً واحداً، بل يعرض خيارات حسب الصلاحيات:

```text
دعوى جديدة
جلسة لملف موجود
إجراء إداري
شركة
عقد
أمر عمل
مهمة مخصصة
```

الأعمال النظامية تفتح شاشاتها الرسمية:

```text
دعوى جديدة → معالج إنشاء الدعوى
شركة → معالج تأسيس الشركة
إجراء → شاشة الإجراء الإداري
أمر عمل → نافذة أمر العمل
```

أما:

```text
مهمة مخصصة
```

فتفتح نموذج المهمة المخصصة.

---

## 21. المهام المتكررة

تدعم لاحقاً:

```text
لا يتكرر
يومي
أسبوعي
شهري
تاريخ مخصص
```

أمثلة:

```text
مراجعة بريد المحكمة كل صباح
نسخ احتياطي أسبوعي
مراجعة صندوق الأرشيف غير المصنف كل يوم خميس
```

---

## 22. الغد والتحضير

تبويب الغد يركز على:

```text
توزيع العمل
تجهيز الملفات
تأكيد المكلفين
طباعة لائحة الغد
```

كل بطاقة في الغد تحتوي أزراراً مثل:

```text
تعيين مكلف
إضافة ملاحظة تحضير
إنشاء أمر عمل
طباعة المهمة
فتح الملف
```

---

## 23. الطباعة

يجب دعم:

```text
طباعة لائحة اليوم
طباعة لائحة الغد
طباعة الأسبوع
طباعة حسب المكلف
طباعة حسب النوع
إخفاء المالية
إظهار النواقص
```

لأن المكلف قد يكون:

```text
مستخدم داخلي
اسم خارجي
```

فالفلاتر تكون حسب:

```text
المكلف
```

وليس فقط المستخدم.

---

## 24. التقويم

التقويم يبقى موجوداً، لكن عند الضغط على يوم:

```text
يفتح ملخص أعمال هذا اليوم
```

يعرض:

```text
جلسات
إجراءات
أوامر عمل
نواقص
مهام مخصصة
تذكيرات
```

مع نفس الصلاحيات والتصنيفات.

---

## 25. الربط مع الأرشيف

الملفات الجارية المستوردة من الأرشيف يجب أن تنتج عناصر عمل.

مثال:

```text
دعوى جارية مستوردة لها جلسة غداً
```

تظهر في:

```text
الغد > جلسات الغد
```

مثال:

```text
دعوى جارية بلا وكالة
```

تظهر في:

```text
يحتاج انتباه
ملفات جارية تحتاج استكمال
```

---

## 26. الصلاحيات المطلوبة

إضافة صلاحيات لاحقاً:

```text
tasks.view
tasks.create
tasks.edit
tasks.result.enter
tasks.templates.manage
tasks.assign
tasks.delete
tasks.print
calendar.view
calendar.manage
```

---

## 27. Audit المطلوب

```text
daily_work.view
task.created
task.assigned
task.result.entered
task.postponed
task.completed
task.cancelled
task.template.created
task.template.updated
task.printed
calendar.day.opened
```

---

## 28. التنفيذ المرحلي المقترح

### المرحلة A — نموذج DailyWorkItem

إنشاء Provider موحد يجمع الأعمال من الجداول الحالية.

### المرحلة B — تبويبات مكتب العمل

```text
اليوم
الغد
الأسبوع
التقويم
```

### المرحلة C — إدخال النتائج

لكل نوع عنصر Result Dialog خاص.

### المرحلة D — المهام المخصصة

```text
Task Templates
Task Outcomes
Manual Tasks
```

### المرحلة E — الطباعة

لائحة اليوم والغد والأسبوع حسب المكلف.

### المرحلة F — الربط بالأرشيف

الملفات الجارية المستوردة تولد عناصر عمل.

### المرحلة G — المساعد الذكي لاحقاً

المساعد يقترح:

```text
ما الذي يحتاج نتيجة اليوم؟
ما الذي يجب تحضيره للغد؟
ما الملفات التي بلا مكلف؟
```

---

## 29. قاعدة ختامية

مكتب العمل لا يعرض كل شيء فقط، بل يدير دورة العمل:

```text
ظهور العمل في تاريخه
إدخال النتيجة
ترحيل تلقائي
إنشاء إجراء لاحق
طباعة وتوزيع
Audit
صلاحيات
```

وهذا يجعله مركز قيادة يومي حقيقي لمكتب المحاماة.

---

## سجل تنفيذ فعلي — تأسيس شاشة مكتب العمل 2026-07-15

تم تنفيذ خطوة تأسيسية من خطة مكتب العمل اليومي:

- إنشاء شاشة `DailyWorkCenterScreen` وربطها بالمسار `/today` بدلاً من لوحة اليوم القديمة.
- اعتماد التبويبات:
  - اليوم
  - الغد
  - الأسبوع
  - التقويم
- جمع عناصر العمل من مصادر حقيقية حالية:
  - جلسات الدعاوى من `uiCasesProvider`
  - المهام اليومية من `tasksByDateProvider`
  - أوامر العمل من `uiWorkOrdersProvider`
  - النواقص المفتوحة من `openDeficienciesProvider` في تبويب اليوم
- عرض العناصر حسب صلاحيات المستخدم.
- تصنيف العناصر إلى جلسات، تحضيرات/إجراءات، أوامر عمل، نواقص، ومهام.
- إضافة زر `إدخال نتيجة` للعناصر المناسبة كتمهيد لمرحلة Result Handlers.
- إضافة زر `إضافة عمل` الذي يفتح المسارات الرسمية الحالية حسب الصلاحيات.
- إضافة تبويب التقويم بحيث يعرض أعمال اليوم المختار.

> هذه شاشة تأسيسية لمكتب العمل. ما زال تنفيذ Result Handlers الكامل، المهام المخصصة، الطباعة حسب المكلف، والترحيل التلقائي سيتم في المراحل التالية.

### سجل تنفيذ فعلي — المهام المخصصة ونتائجها الأساسية 2026-07-15

تم تنفيذ خطوة عملية من مرحلة المهام المخصصة:

- تفعيل خيار `مهمة مخصصة` من زر `إضافة عمل` داخل مكتب العمل وفق صلاحية `tasks.create`.
- إضافة حوار لإنشاء مهمة مخصصة بتاريخ ووقت اختياري ومكلف نصي وأولوية وملاحظات.
- حفظ المهمة في جدول `DailyTasks` الحالي بنوع `manual` دون تعديل قاعدة البيانات.
- تسجيل Audit عند إنشاء مهمة مخصصة أو محاولة إنشائها دون صلاحية.
- تفعيل إدخال نتيجة للمهمة المخصصة من مكتب العمل وفق صلاحية `tasks.result.enter`.
- دعم نتائج أساسية:
  - تم الإنجاز
  - تأجيل إلى تاريخ جديد
  - إلغاء المهمة
- تسجيل Audit عند إكمال أو تأجيل أو إلغاء المهمة.

> المتبقي لاحقاً: قوالب المهام المخصصة ونتائجها القابلة للتخصيص وآثارها التلقائية الكاملة.

### سجل تنفيذ فعلي — طباعة لوائح العمل 2026-07-15

تم تنفيذ خطوة عملية من مرحلة الطباعة في مكتب العمل:

- تفعيل زر طباعة/تصدير لائحة العمل في شاشة مكتب العمل.
- دعم طباعة:
  - لائحة عمل اليوم
  - لائحة عمل الغد
  - لائحة عمل الأسبوع
  - لائحة اليوم المحدد من التقويم
- توليد PDF يتضمن:
  - النوع
  - العنوان
  - الوصف/الملف
  - التاريخ
  - الوقت
  - المكلف
  - الأولوية
  - الحالة
- تسجيل Audit عند تصدير/طباعة لائحة العمل.

> المتبقي لاحقاً: خيارات طباعة حسب المكلف، حسب النوع، وإخفاء/إظهار الأقسام الحساسة مثل المالية وفق إعدادات الطباعة.

### سجل تنفيذ فعلي — خيارات طباعة لائحة العمل 2026-07-15

تم تحسين طباعة لوائح مكتب العمل:

- إضافة نافذة خيارات قبل التصدير.
- دعم فلترة اللائحة حسب المكلف، سواء كان مستخدماً داخلياً أو اسماً خارجياً.
- دعم فلترة اللائحة حسب نوع العمل: جلسة، أمر عمل، نقص، مهمة، إجراء، وغيرها.
- عرض عدد عناصر اللائحة قبل التصدير.
- إبقاء تسجيل Audit عند التصدير.

> المتبقي لاحقاً: خيارات إظهار/إخفاء الأقسام الحساسة، وتصميم PDF نهائي بهوية ميزان.

### سجل إصلاح بناء — 2026-07-17

استجابةً لأخطاء بناء Windows بعد توسيع مكتب العمل، تم تثبيت العناصر الناقصة في `DailyWorkCenterScreen`:

- إضافة مولّد PDF داخلي للائحة العمل اليومية/الأسبوعية.
- تثبيت نافذة إضافة مهمة مخصصة مع التاريخ والوقت والمكلف والأولوية.
- تثبيت نافذة نتيجة المهمة: إنجاز، تأجيل مع سبب وتاريخ جديد، أو إلغاء مع سبب.
- ربط العمليات بمستودع المهام الموجود وتسجيل Audit دون إنشاء مسار عمل بديل.

> الإصلاح هدفه إغلاق أخطاء البناء مع الحفاظ على منطق المهام الحالي وعدم تغيير مسارات الملفات الرسمية.

### سجل تنفيذ فعلي — إدخال أرشيف جارٍ من مكتب العمل 2026-07-17

تمت إضافة خيار `إدخال أرشيف جارٍ` إلى زر `إضافة عمل` داخل مكتب العمل.

- يفتح الخيار معالج الأرشيف محدداً على `أرشيف جارٍ`.
- الهدف هو تمكين المحامي من إدخال ملف قديم حي مباشرة من سياق العمل اليومي عندما يكتشف أن ملفاً قديماً ما زال يحتاج متابعة أو موعداً قادماً.
- لم تتم إضافة خيار الأرشيف المنتهي هنا حتى يبقى مكتب العمل مركزاً للملفات الحية والعمل القادم.

> هذا يدعم القاعدة العملية: ما يدخل من مكتب العمل يجب أن يكون قابلاً للتأثير على اليوم/الغد/التقويم.

### قرار تصميمي — دورة حياة أمر العمل ونتيجته 2026-07-17

تم الاتفاق على أن أمر العمل ليس مجرد مهمة، بل تكليف تنفيذي له دورة حياة واضحة ويجب أن يظهر في مكتب العمل لأنه يملك تاريخ تنفيذ إلزامي.

#### إنشاء أمر العمل

كل أمر عمل يجب أن يتضمن:

- نوع الأمر.
- الملف المرتبط، أو أمر عام بلا ارتباط.
- المكلف:
  - مستخدم داخلي.
  - أو شخص خارجي / معقب بنص حر حالياً.
- تاريخ التنفيذ الإلزامي.
- المطلوب تنفيذه.
- مرفقات تسليم اختيارية.
- مصروف متوقع اختياري، لا يدخل المالية كمصروف فعلي إلا عند النتيجة.
- إمكانية طباعة أمر العمل PDF.

#### معنى ربط أمر العمل بملف

ربط أمر العمل يعني أن نتيجته ستدخل في سجل ذلك الملف عند الاعتماد:

- دعوى: نتيجة مراجعة، ضبط، موعد، نقص، مستند، مصروف.
- إجراء / معاملة: خطوة تنفيذ، موعد مراجعة، مستند، مصروف.
- شركة: مرحلة تأسيس أو متابعة، مستند، موعد، مصروف.
- عقد: متابعة عقد، مستند، تذكير لاحق، مصروف.
- وكالة: سند أو متابعة وكالة.
- أمر عام: نتيجة محفوظة في سجل أوامر العمل فقط، مع مصروف عام إن وجد.

#### نتيجة أمر العمل

عند تاريخ التنفيذ تظهر إمكانية إدخال نتيجة الأمر من مكتب العمل أو شاشة أوامر العمل.

أنواع النتيجة:

```text
تم التنفيذ
تم التنفيذ جزئياً
تعذر التنفيذ
تأجيل
إلغاء
```

حقول النتيجة:

- ماذا حدث؟
- ملاحظات.
- مرفقات النتيجة.
- هل يوجد موعد لاحق؟
- تاريخ الموعد اللاحق.
- المطلوب في الموعد اللاحق.
- هل تم دفع مصروف فعلي؟
- نوع المصروف.
- المبلغ.
- من دفع: المكتب / الموكل / المعقب.
- إيصال إن وجد.

#### الاعتماد

إدخال النتيجة لا يعني اعتمادها.

دورة النتيجة:

```text
نتيجة مدخلة
↓
اعتماد من المحامي / المسؤول
```

عند الاعتماد فقط يتم تحديث الملف المرتبط بشكل نهائي:

- إضافة حدث Timeline.
- حفظ المرفقات كمستندات مرتبطة.
- إنشاء مصروف فعلي عند وجود مبلغ مدفوع.
- إنشاء موعد لاحق أو مهمة متابعة عند وجود تاريخ لاحق.
- إغلاق نواقص عند الاقتضاء.

#### طلب التصحيح

تم الاتفاق على دعم حالة:

```text
يحتاج تصحيح
```

إذا كانت نتيجة المكلف غير كافية أو مرفقاتها ناقصة.

أمثلة:

- الصورة غير واضحة.
- الإيصال ناقص.
- لم يذكر رقم الأساس.
- النتيجة لا تكفي لاعتماد الملف.

في هذه الحالة يرجع أمر العمل للمكلف مع ملاحظة تصحيح.

#### حالات أمر العمل المعتمدة

```text
مفتوح
نتيجة مدخلة
معتمد
مؤجل
ملغى
يحتاج تصحيح
```

> هذه الخطة ستستخدم عند إعادة ضبط شاشة أوامر العمل ونتائجها وربطها النهائي بملفات المكتب والمالية.
