# 🛠️ دستور ودليل تطوير مشروع "إدارة مكتب محاماة سوري" (Lawyer Office Development Guidelines & Constitution)

> **هذا المستند هو مرجع ودستور إلزامي للمطور (الوكيل الذكي) لضمان استمرارية الجودة، تطابق الكود مع التوثيق، وتطبيق أعلى معايير الهندسة البرمجية في نظام إدارة مكتب المحاماة السوري (Offline-First V6.2).**
> تم نسخ المبادئ والضوابط الأساسية من مستودع العقارات (`sweeda-real-estate-office-elc`) وتكييفها لتناسب طبيعة النظام القضائي غير المتصل بالسيرفر حالياً.

---

## 🔄 دورة حياة التعديل والتطوير (Workflow)
يُمنع إغلاق أي مهمة أو إبلاغ المستخدم بالانتهاء إلا بعد استكمال الخطوات الثلاث التالية بالترتيب الدقيق:

### 1️⃣ المرحلة البرمجية (Coding Phase)
- تنفيذ التعديلات المطلوبة في ملفات كود Dart (`.dart`) ومخططات قاعدة البيانات (`Drift / SQLite`).
- التأكد المطلق من عدم وجود أخطاء برمجية (Syntax & Type Errors) وتشغيل أدوات التحليل (`flutter analyze`).
- اختبار المنطق التجاري والقانوني (Business & Legal Logic) لضمان عدم وجود ثغرات في دورة الحياة الموحدة للعمليات، الترقيم السنوي الآمن، الماليّة الموحدة، أو نظام النواقص التلقائي.

#### ⛔ بوابة الرفع الإلزامية (Pre-Push Gate)

**يُمنع الرفع قبل اجتياز هذه البوابة كاملة.** كل بند فيها نتج عن عطل
وقع فعلاً وكلّف المستخدم دورة بناء فاشلة.

| # | الفحص | الأمر | لماذا |
|---|---|---|---|
| 1 | تحليل الأنواع | `flutter analyze` | **لا بديل عنه** — الفحص النحوي لا يلتقط عدم تطابق الأنواع |
| 2 | الأعضاء والمزودات | `dart run tool/audit/audit_members.dart` | يلتقط التعريف المكرر والعضو المفقود وسوء استعمال `family` |
| 3 | الاختبارات | `flutter test` | يحرس الترحيل وسلامة النسخ الاحتياطي |
| 4 | البناء الفعلي | `flutter run -d windows` | الحكم النهائي |

**إن تعذّر تنفيذ أي فحص، يُصرَّح بذلك للمستخدم نصاً قبل الرفع** —
لا يُقال «تم التحقق» عن فحص لم يُنفَّذ. الفحص الساكن وحده ليس
تحققاً كافياً، والادعاء بغير ذلك أخطر من عدم الفحص.

#### 🔍 قواعد التعديل الآمن

نشأت هذه القواعد من أخطاء متكررة في التعديل النصي على الكود:

1. **قبل حذف أي رمز، أحصِ كل مواضع استعماله.**
   حُذف `caseItem` و`nextSession` من دالتين اعتماداً على تحذير
   `unused_local_variable` يخصّ دالة واحدة، بينما كانت الأخرى
   تستعملهما في تسعة مواضع.
   → `grep -n "الرمز" الملف` وافحص **كل** نتيجة، لا التي ورد
   ذكرها في التحذير.

2. **قبل إضافة أي رمز عام، تأكد أنه غير معرّف.**
   أُضيف `activeCourtsProvider` وكان موجوداً أصلاً كـ `family`.
   → `grep -rn "الاسم" lib/` قبل أي `final` جديد على مستوى الملف.

3. **الاستبدال النصي يجب أن يشمل سياقاً مميِّزاً.**
   `_submitClient` و`_submitOpponent` يبدآن بالسطر نفسه، فأصاب
   الاستبدال الدالة الخطأ: حوار الموكل صار يشير إلى حقول لا
   يملكها، وحوار الخصم بقي بلا تحقّق.
   → ضمّن اسم الدالة أو سطراً فريداً في نص البحث، وتحقّق بعد
   التعديل أن الكتلة استقرّت في الصف المقصود.

4. **بعد أي تعديل، افحص الملف كاملاً لا الكتلة المعدَّلة.**
   الأداة `audit_members` تكشف انتقال الكتل بين الصفوف.

5. **لا تُعمِّم إصلاحاً دون فحص نطاقه.**
   المرفقات تُخزَّن مشفّرة والقوالب لا؛ تعميم فكّ التشفير على
   الاثنين كان سيكسر القوالب. افحص كل موضع قبل تطبيق النمط نفسه.

### 2️⃣ المرحلة التوثيقية (Documentation Phase) ⚠️ **(إلزامية)**
- مراجعة ملف `docs/EXECUTION_PLAN.md`: إذا تم تغيير أي حقل في قاعدة البيانات، تعديل هندسي، أو إضافة ميزة جديدة، يجب تحديث المواصفات ووثيقة التنفيذ فوراً.
- مراجعة وتحديث حالة المهام في خطة التنفيذ (نقل الميزة من "قيد التنفيذ" أو "مجدولة" إلى "مكتملة") بعد التأكد التام من عملها.
- توثيق أي تغييرات في الجداول المرجعية، المحركات الخلفية (Engines)، أو إعدادات الطباعة القانونية.

### 3️⃣ مرحلة الرفع (Deployment Phase) ⚠️ **(إلزامية)**
- تنفيذ سلسلة أوامر الرفع لضمان وصول التعديلات إلى المستودع مباشرة في نهاية كل مهمة:
  ```bash
  git add .
  git commit -m "وصف دقيق للتعديلات المنجزة ومطابق الخطة"
  git push origin main
  ```

#### ✅ تأكيد الوصول بعد الرفع (إلزامي)

`git push` الناجح لا يعني أن التعديل بلغ جهاز المستخدم. وقعت حالتان
ظهر فيهما الخطأ نفسه بأرقام الأسطر ذاتها بعد إصلاحه ورفعه:

```bash
git fetch origin
git log --oneline origin/main -1     # يطابق آخر commit محلي؟
git show origin/main:المسار | sed -n 'السطر p'   # تحقق من المحتوى
```

**عند تكرار المستخدم لخطأ سبق إصلاحه:**
1. لا تفترض بقاء العلة في الكود، وتحقّق من `origin/main` مباشرة.
2. **أرقام الأسطر المتطابقة تماماً دليل على أن الملف لم يتغيّر**، لا
   على أن الإصلاح فشل.
3. إن كان المستودع سليماً، فالمشكلة في وصول التعديل. الحل الحاسم:
   ```bash
   git fetch origin && git reset --hard origin/main && git log --oneline -1
   ```
   مع تنبيه المستخدم أن هذا يمحو أي تعديل محلي غير مرفوع.

---

## 🚫 محظورات تقنية
- **لا** تترك أي `print` أو `debugPrint` في الكود النهائي المنتظم (استخدم نظام السجلات `ActivityLog` أو أدوات المراقبة المعتمدة).
- **لا** تضف نصوصاً ثابتة (Hardcoded) في الواجهات؛ كل العناوين والقوائم القانونية يجب أن تمر عبر الجداول المرجعية (Lookups) أو ملفات الثوابت والتوطين.
- **لا** تغير في بنية الجداول دون تحديث مخطط قاعدة بيانات Drift (`schema.dart`) وتشغيل `build_runner` لضمان توافق الأكواد المولدة (`database.g.dart`).
- **لا** تضف عموداً إلى `schema.dart` دون رفع `schemaVersion` وإضافة `m.addColumn` مقابلة في `onUpgrade`. تعديل المخطط وحده لا يصل إلى قواعد المستخدمين القائمة، بينما يولّد Drift عبارة `INSERT` تتضمن العمود الجديد، فيفشل الإدخال بخطأ `SQLite error 1 (no such column)`. هذا العطل أصاب جدول الوكالات فعلياً وكان ينتظر في `work_orders`. الاختبار `test/schema_migration_test.dart` يحرس هذه القاعدة.
- **لا** تتجاوز قاعدة "دورة الحياة الموحدة" لأي كيان تشغيلي (`scheduled`, `in_progress`, `completed`, `postponed`, `cancelled`).
- **لا** تحذف المرفقات أو الملفات المرتبطة بالكيانات دون احترام قواعد القيود الخارجية (`ON DELETE RESTRICT` أو `CASCADE` حسب التصميم المعتمد).
- **لا** تُبلغ المستخدم بنجاح عملية لم تُنفَّذ. كل رسالة «تم…» يجب أن
  يسبقها عمل حقيقي: `await` على مستودع أو خدمة، أو تعديل حالة. وُجدت
  في المشروع ثماني حالات من هذا النمط، أخطرها زر «استعادة نسخة
  احتياطية» كان يعرض «تمت الاستعادة» دون استرجاع أي بيانات، وزر
  «تطبيق الفلاتر» لا يفلتر شيئاً. الأداة `tool/audit/audit_handlers.dart`
  تحرس هذه القاعدة (`FAKE_SUCCESS`).
- **لا** تترك معالج تفاعل بجسم فارغ أو بتعليق يصف نيّة غير منفَّذة
  مثل `onPressed: () { /* إتمام المرحلة */ }`. إن لم يكن المنطق
  جاهزاً فعطّل الزر (`onPressed: null`) مع `tooltip` يوضّح السبب،
  فالزر الذي لا يفعل شيئاً أسوأ من زر غائب.
- **لا** تمرّر فهرس قائمة معروضة مكان معرّف قاعدة بيانات. كان
  `courtId` يُملأ بـ `_courtNames.indexOf(gov)` وهو ترتيب في قائمة
  واجهة، بينما العمود مفتاح أجنبي يشير إلى `Courts.id`، فانهار
  قيد المفتاح الأجنبي عند الحفظ.
- **لا** تعرّف المحكمة بمحافظتها وحدها. المحكمة = **نوعها +
  محافظتها + غرفتها**، و«حمص» ليست جواباً عن سؤال «أمام أي محكمة؟».
  النوع يُخزَّن في `court_kind` بمعرّف من `CourtCatalog`، لا بنص
  معروض ولا بدرجة محشورة في `sub_type`. كان اختيار المحكمة محافظةً
  مجرّدة، فظهرت في شاشة الاستئناف «حمص» بلا درجة ولا غرفة.
- **لا** تشتقّ خيارات الانتقال بين المراحل من `enum` عرضٍ عام.
  كانت شاشة النقل تعرض `CasePhaseType.values` كاملة، فظهرت
  «جلسات» و«إثبات» و«حكم» كأنها محاكم يُنتقل إليها، وظهر «صلح»
  خياراً بعد «استئناف». مسار الطعن مُعرَّف صعوداً في
  `CourtCatalog.nextStagesFrom`، ويحرسه `CaseRepository`
  برفض أي انتقال خارجه. الاختبار `test/court_catalog_test.dart`
  يمنع الانحدار إلى درجة أدنى.
- **لا** تُكرّر قائمة مرجعية قانونية في شاشة. كانت شاشة الأرشيف
  تحمل خارطة محاكم يدوية انحرفت عن الواقع: أدرجت «محكمة عمالية»
  ضمن القضاء الإداري، وأغفلت الأحداث والتنفيذ. تُشتق الآن من
  `CourtCatalog` فيبقى المصدر واحداً.
- **لا** تحصر قائمة المحافظات في بعضها. كان حوار المحاكم يعرض سبع
  محافظات فقط، فتعذّر تسجيل محكمة في إدلب أو الرقة أو دير الزور أو
  الحسكة أو طرطوس. المرجع الوحيد `AppConstants.syrianGovernorates`.
- **لا** تفتح مرفقاً بمساره الخام. المرفقات تُخزَّن مشفّرة (AES)
  بلاحقة `.enc` عبر `saveAttachment`، فتسليم المسار إلى `OpenFilex`
  يفتح ملفاً غير مقروء. استخدم `AttachmentService` التي تفكّ
  التشفير إلى ملف مؤقت. **استثناء:** القوالب تُحفظ عبر `saveTemplate`
  دون تشفير عن قصد، فتُفتح مباشرة.
- **لا** تبتلع خطأً في عملية حرجة بـ `catch (_) {}` ثم تُكمل كأن
  شيئاً لم يكن. الاستعادة والنسخ الاحتياطي والحفظ يجب أن تُظهر سبب
  الفشل. إرجاع `false` مبهم بدل رسالة واضحة عيب مماثل.
- **لا** تشترط نوع `Ref` في خدمة لا تحتاج نطاق المزوّدات. الخدمة
  التي تحتاج قاعدة البيانات فقط يجب أن تستقبل `AppDatabase`؛
  اشتراط `Ref` يمنع استدعاءها من الواجهة (التي تملك `WidgetRef`)
  ويعقّد اختبارها.

---

## 🎯 مبدأ الدقة المتناهية (إلزامي — مقتبس ومحفوظ من دستور العقارات ومُطبق بصرامة هنا)

كل مهمة تُنفَّذ بدقة متناهية وتحليل كامل مهما أخذت من وقت وجهد، **دون اختصار ودون حلول جزئية**. السببان الموجبان:

1. **سبب يخص المشروع:** النظام يتعامل مع ملفات دعاوى، مواعيد جلسات قانونية حساسة، وأمور مالية تخص مكتب المحاماة — أي خطأ في الحسابات، الترقيم السنوي، أو مواعيد الجلسات يسبب أضراراً قانونية حقيقية، فلا مجال لتعديلات متسرعة أو غير مكتملة.
2. **سبب يخص كفاءة العمل:** الحلول الجزئية والاختصارات تجبر صاحب المشروع على فتح محادثات جديدة لنفس الموضوع وإعادة شرح السياق من الصفر — وهذا استنزاف للموارد والوقت دون داعٍ. الحل الكامل من أول مرة أوفر دائماً.

### قواعد التطبيق العملي لهذا المبدأ:
- **لا افتراضات حول مخطط قاعدة البيانات المحلي (`Drift / SQLite / SQLCipher`):** قبل أي تعديل يمس الجداول أو العلاقات، يُتحقق من تناغم تعريفات Drift (`schema.dart`)، القيود الخارجية، الفهارس، وحالة توليد الأكواد.
- **تتبع كامل للتبعيات (Full Dependency Tracking):** عند تعديل أي كيان (Entity)، دالة، أو حقل قاعدة بيانات، تُحصر كل الجهات المستدعية (الجداول المترابطة، DAOs، Repositories، Riverpod Providers، الشاشات، ومحركات النواقص والخط الزمني) وتُحدَّث جميعها في نفس المهمة.
- **معالجة جذر المشكلة لا عرضها:** إن كشف التحليل ثغرات مجاورة في نفس النطاق، تُذكر صراحة في التقرير ويتم التعامل معها فوراً أو إدراجها ضمن المهام ذات الأولوية القصوى — لا تُترك مكتومة.
- **لا تنفيذ دون تصريح واضح للمهام المحورية:** أي تعديل معماري كبير يتطلب توضيح خطوة بخطوة لما سيتم إنجازه والالتزام بالمخطط المعتمد.
- **الإنجاز الحقيقي فقط:** لا يُعلن اكتمال ميزة إلا بعد اكتمال سلسلتها كاملة (`Database Schema` + `Service/Engine` + `Repository` + `Riverpod Provider` + `UI/Screens` + `Documentation` + `Git Push`) وتحديد ما تم إنجازه بدقة متناهية.

---

---

## 🧠 مبدأ الجاهزية للذكاء الاصطناعي (AI-Readiness Principle)

> **كل تعديل يُنفَّذ في التطبيق يجب أن يُراعي بنية البيانات اللازمة لتدريب وتشغيل الذكاء الاصطناعي مستقبلاً.**

### القاعدة الأساسية:
التطبيق ليس فقط أداة إدخال وعرض — هو **محرك جمع بيانات تدريبية** للذكاء الاصطناعي. كل تفاعل للمستخدم يولّد بيانات منظمة يمكن للـ AI التعلم منها.

### 1. بنية القالب والنسخة (Template → Instance Pattern)

كل كيان مستندي في التطبيق يتبع نمط **القالب → النسخة المخصصة**:

```
القالب (Template)              النسخة المخصصة (Instance)
┌──────────────────┐           ┌──────────────────────┐
│ نموذج Word أصلي  │─── نسخ ──→│ نسخة باسم العميل     │
│ لا يتغير أبداً   │           │ تُحفظ ContractVersion │
│ {{متغيرات}}      │─── ملء ──→│ قيم حقيقية           │
│ تصنيف قانوني     │           │ مرتبطة بملف مكتب     │
└──────────────────┘           └──────────────────────┘
```

**ينطبق على:** العقود، صحف الدعاوى، المذكرات، الإجراءات الإدارية، عقود الشركات، الوكالات.

**الجداول المعتمدة:**
- `ContractTemplates` — القوالب (مصدر: ready/imported/generated)
- `ContractTemplateVariables` — المتغيرات المكتشفة {{...}} في كل قالب
- `ContractInstanceVariables` — قيم المتغيرات لكل نسخة عقد
- `ContractTemplateUsageLog` — سجل كل تفاعل (اختيار، ملء، تحرير، حفظ)
- `sourceTemplateId` في `Contracts` — ربط النسخة بالقالب المصدر

### 2. البيانات التدريبية (Training Data Structure)

كل عملية تسجيل نتيجة تُولّد JSON منظم للتعلم:

```json
{
  "template_id": 5,
  "template_type": "عقد بيع قطعي لعقار",
  "variables_detected": ["البائع", "المشتري", "الثمن", "العقار"],
  "variables_filled": {
    "البائع": {"value": "أحمد بن محمد", "source": "party_1", "method": "auto"},
    "الثمن": {"value": "500000000", "source": "manual", "method": "manual"}
  },
  "parties": [{"role": "البائع", "person_id": 12, "capacity": "أصيل"}],
  "word_edits_count": 3,
  "corrections": [{"variable": "الثمن", "original": "0", "corrected": "500000000"}]
}
```

### 3. قواعد AI-Ready الإلزامية

| القاعدة | التفصيل |
|---------|---------|
| **سجل كل تفاعل** | كل اختيار قالب، ملء متغير، تحرير Word → يُسجَّل في `UsageLog` |
| **ميّز طريقة الملء** | `auto` (من الأطراف) / `manual` (يدوي) / `ai` (ذكاء اصطناعي مستقبلاً) |
| **سجّل التصحيحات** | إذا عدّل المحامي قيمة ملأة تلقائياً → `wasCorrected = true` (تعلم من الأخطاء) |
| **اربط القالب بالنسخة** | `sourceTemplateId` إلزامي عند إنشاء عقد من قالب |
| **لا تمسح القوالب** | القالب الأصلي لا يُحذف أبداً — يُعطَّل فقط (`isDisabled`) |
| **بنية متسقة** | كل القوالب تتبع نفس نمط المتغيرات `{{اسم_المتغير}}` |

### 4. سيناريوهات AI المستقبلية

```
المرحلة 1: RAG على القوالب
  → "اكتبلي عقد إيجار تجاري بدمشق" → يبحث القوالب → يملأ المتغيرات

المرحلة 2: التعلم من التصحيحات
  → المحامي يصحح قيمة الثمن → AI يتعلم النمط → يقترح قيمة أدق

المرحلة 3: التصنيف التلقائي
  → رفع 50 مستند → AI يصنفها (عقد/دعوى/إجراء) → يربطها بالملفات

المرحلة 4: المساعد الذكي
  → "شو عندي بكرا؟" → يبحث المهام → يلخص اليوم
  → "جهّزلي مذكرة لدعوى الإيجار رقم 123" → يبحث القوالب → يملأ البيانات
```

---

## 🎯 المعالج الشامل لتسجيل النتائج (Unified Result Wizard)

> **الفكرة الجوهرية:** المحامي يُدخل النتيجة مرة واحدة → التطبيق يُنفذ 6 عمليات تلقائياً في معاملة ذرية.

### الفلسفة:
هذا المعالج هو **قلب التطبيق النابض** — يُحوّل عمل المحامي من إدخال يدوي متكرر إلى **ضغطة زر واحدة** تُطلق سلسلة أتمتة كاملة.

### النتائج الخمس المعتمدة:
```
1. ✅ منجز نهائياً            → إغلاق المهمة + تسجيل خط زمني
2. ✅ منجز + موعد جديد        → إغلاق + توليد مهمة متابعة + تحديث next_session
3. ⏳ مؤجل بسبب              → تأجيل + توليد موعد جديد + تسجيل سبب
4. ❌ متعذر بسبب             → إلغاء + فتح نقص + تسجيل سبب
5. 🚫 ملغى بسبب              → إلغاء + تسجيل سبب
```

### سلسلة الأتمتة (Transaction):
```
ضغطة "حفظ" → معاملة ذرية واحدة:
├── 1. تسجيل النتيجة في DailyTasks
├── 2. تسجيل مصروف في Expenses (إن وُجد + سياق دعوى)
├── 3. تحديث حالة الجلسة في CaseSessions
├── 4. تحديث next_session_date في Cases
├── 5. توليد مهمة متابعة في الموعد القادم
├── 6. إغلاق النواقص المرتبطة (next_session_date, followup_pending)
├── 7. تسجيل في ActivityLog + AuditLog
└── 8. [AI] تسجيل بيانات التدريب في UsageLog
```

### حقول المعالج الإلزامية:
| الحقل | النوع | الإلزام | الأتمتة |
|-------|-------|---------|---------|
| عنوان العمل | نص | ✅ | عنوان المهمة |
| النتيجة | اختيار (5) | ✅ | تحديد lifecycle status |
| الموعد القادم | تاريخ + وقت | مشروط | توليد مهمة + تحديث جلسة |
| حضور الموكل | boolean | ❌ | تسجيل في الجلسة |
| حضور الخصم | boolean | ❌ | تسجيل في الجلسة |
| حضور محامي الخصم | boolean | ❌ | تسجيل في الجلسة |
| المصاريف | رقم | ❌ | تسجيل مصروف + ربط بالصندوق |
| المرفقات | ملف | ❌ | حفظ مستند مرتبط |
| قرار المحكمة | اختيار | ❌ | تصنيف للحظة القانونية |
| ملاحظات | نص | ❌ | تسجيل في الجلسة + الخط الزمني |

### التوسع المستقبلي:
- **العقود:** تسجيل تجديد → توليد تذكير انتهاء جديد + تحديث حالة العقد
- **الشركات:** تسجيل مرحلة → إغلاق مرحلة + فتح التالية + توليد مهمة
- **الإجراءات:** تسجيل مراجعة → تحديث Checklist + موعد المراجعة القادمة

---

## 🎨 الهوية البصرية الموحدة (Visual Identity)

### الألوان المعتمدة — كحلي + ذهبي فقط:
```
AppColors.primaryNavy   = #1A2332  (الكحلي الرئيسي)
AppColors.secondaryGold = #C9A961  (الذهبي الرئيسي)
AppColors.cardBackground = #FFFFFF  (خلفية البطاقات)
AppColors.textPrimary   = #2C3E50  (النص الرئيسي)
AppColors.textSecondary = #6C757D  (النص الفرعي)
```

### ألوان الحالة الوظيفية (مسموحة فقط للدلالة على المعنى):
```
AppColors.success  = #28A745  (مكتمل/نشط)
AppColors.error    = #DC3545  (ملغى/طوارئ)
AppColors.warning  = #FFC107  (تنبيه/مؤجل)
AppColors.info     = #17A2B8  (جاري/معلومات)
```

### المحظورات البصرية:
- ❌ لا ألوان خارج `AppColors` (لا `AppConstants.primaryNavy` ولا `Colors.blue` مباشرة)
- ❌ لا `Card(` عادي — فقط `GlassmorphicCard(`
- ❌ لا `borderRadius: BorderRadius.circular(8)` — فقط `16` أو أعلى
- ❌ لا ألوان عشوائية لكل مجموعة — التمييز بالنص والأيقونة لا باللون

---

## 📂 فلسفة "غلاف + مستند" (Wrapper + Document Philosophy)

> **ملف أي كيان في التطبيق هو غلاف إداري/قانوني للفهرسة والمتابعة — وليس إعادة كتابة لمضمون المستند نفسه.**

### القاعدة:
```
التطبيق يحفظ:                    المستند يحفظ:
├── رقم الملف                    ├── الثمن/القيمة
├── التصنيف القانوني             ├── الشروط التفصيلية
├── الأطراف + صفاتهم             ├── بنود الضمان والفسخ
├── التواريخ                     ├── أوصاف محل العقد
├── الحالة                       ├── الغرامات والجزاءات
├── النسخة (Word/PDF)            └── كل التفاصيل القانونية
├── الملفات المرتبطة
├── المالية
└── سجل الأحداث
```

### ينطبق على جميع الأنواع:
| النوع | الغلاف | المستند |
|-------|--------|---------|
| دعوى | رقم أساس + محكمة + أطراف | صحيفة الدعوى + المذكرات |
| عقد | تصنيف + أطراف + تاريخ | نموذج Word/PDF |
| شركة | اسم + نوع + شركاء | عقد التأسيس + النظام الأساسي |
| إجراء | نوع + جهة + حالة | الاستدعاء + المعاملة |
| وكالة | نوع + موكل + وكيل | سند الوكالة |

### النواقص المسموح بها:
- ✅ "لا توجد نسخة عقد مرفقة" (نقص إداري)
- ✅ "لم يُحدد موعد الجلسة القادمة" (نقص متابعة)
- ❌ "لم تدخل الثمن" (هذا داخل المستند، ليس من اختصاص التطبيق)
- ❌ "لم تدخل شروط التسليم" (هذا داخل المستند)

---

## 📐 هندسة القائمة الجانبية (Sidebar Architecture)

### الهيكل المعتمد — 4 مجموعات، كلها قوائم منسدلة:
```
── العمل اليومي ──
  ▼ لوحة اليوم | الأجندة | ⭐ عمل جديد | أوامر العمل

── الملفات والأشخاص ──
  ▼ ملفات المكتب (جارية | منتهية | وكالات)
  ▼ الأشخاص والجهات (الكل | موكلون | خصوم | محامو خصوم | شركات)

── الأدوات والمرجع ──
  ▼ المالية (لوحة | اتفاقيات | دفعات | مصاريف | صندوق)
  ▼ النماذج القانونية (الكل | عقود | دعاوى | مذكرات | إجراءات)
  ▼ البحث والتقارير (بحث | طباعة)
  ▼ المكتبة القانونية (الكل | قوانين | اجتهادات | مفضلة)

── الإدارة ──
  ▼ إدارة المكتب (أرشيف | إعدادات)
```

### قواعد الفلترة من السايدبار:
- الأشخاص: `?role=client|opponent|opponentLawyer|legalEntity` → يفتح التبويب المناسب
- المالية: `?tab=agreements|payments|expenses|cashbox` → يفتح التبويب المناسب
- النماذج: `?category=عقد|لائحة دعوى|مذكرة|طلب إداري` → يفلتر القائمة
- المكتبة: `?section=laws|precedents|favorites` → يفتح القسم المناسب

### القواعد:
- كل العناصر الرئيسية = **قوائم منسدلة** (لا عناصر منفردة)
- كل الأيقونات = **كحلي + ذهبي فقط** (لا ألوان قوس قزح)
- **عمل جديد** = زر بارز (gradient كحلي + أيقونة ذهبية)
- مكان محجوز **للمساعد الذكي** 🤖 (يُفعّل مع Phase AI)

---

**تذكير ذاتي:** "العمل غير الموثق وغير المرفوع هو عمل غير موجود."
