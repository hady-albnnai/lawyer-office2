# ملف التسليم — 4 آب 2026

> **لمن يقرأ:** هذا الملف مكتوب للوكيل الذكي الذي سيكمل العمل في
> المحادثة القادمة، ولصاحب المكتب (محامٍ سوري) ليعرف أين وصلنا.
> اقرأه كاملاً **قبل** لمس أي ملف. مكتوب بصيغة «ماذا حدث فعلاً»
> لا «ماذا كان مخططاً».

---

## 0. الحالة في سطر واحد

آخر عمل مرفوع ومؤكَّد على GitHub هو **`a35de2f`** — إعادة تعريف
المحكمة في النظام لتطابق درجات التقاضي وأنواع المحاكم السورية.
الكود سليم، صفر أخطاء تحليل، 187 اختباراً ناجحاً.
**لم يجرّبه المستخدم على جهازه بعد.**

---

## 1. الأوامر التي يجب أن ينفّذها المستخدم الآن

```cmd
cd C:\Users\ASUS\Desktop\lawyer-office2
git fetch origin && git reset --hard origin/main && git log --oneline -1
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter run -d windows
```

**يجب أن يظهر `a35de2f`.**

⚠️ `git reset --hard` يمحو أي تعديل محلي غير مرفوع. هذا مقصود:
سبب تعطّل المستخدم مرتين سابقاً كان نسخة محلية لا تطابق GitHub.

---

## 2. بيئة العمل — معلومات حرجة

### جهاز المستخدم
| البند | القيمة |
|---|---|
| المسار | `C:\Users\ASUS\Desktop\lawyer-office2` |
| Flutter | 3.44.8 • Dart 3.12.2 |
| SDK | `C:\src\flutter` |
| المستودع | `https://github.com/hady-albnnai/lawyer-office2` |

### التوكن

⚠️ **لا يُكتب التوكن في هذا الملف ولا في أي ملف داخل المستودع.**
حاولتُ ذلك فعلاً في أول نسخة من هذا التسليم، فرفض GitHub الرفع
عبر Push Protection — والرفض في محلّه: التوكن المكتوب في ملف
مرفوع يصير مكشوفاً لكل من يصل إلى المستودع، ويبقى في سجل الـ
commits حتى بعد حذفه.

**التوكن القديم `ghp_5xgt…` أُبطل.** التوكن الحالي يُسلّمه
المستخدم في المحادثة عند الحاجة، ويبقى في الذاكرة فقط.

الـ remote يُمسح دورياً من بيئة الوكيل، فيُعاد إضافته هكذا
(يُستبدل `<TOKEN>` بالقيمة التي يعطيها المستخدم):

```bash
git remote add origin https://<TOKEN>@github.com/hady-albnnai/lawyer-office2.git
```

> إن ظهر خطأ `GH013 / Push cannot contain secrets` مستقبلاً، فهذا
> يعني أن سرّاً تسرّب إلى ملف — **يُحذف السرّ ويُعاد بناء الـ
> commit، ولا يُستعمل رابط الاستثناء لتجاوز الحماية.**

### بيئة الوكيل — ما تغيّر جذرياً في هذه الجلسة

**في الجلسات السابقة لم يكن تشغيل `flutter analyze` ولا
`flutter test` ممكناً**، وكان هذا سبب ثلاث دورات بناء فاشلة
أُرسلت للمستخدم. في هذه الجلسة حُلّت المشكلة:

1. تنزيل Flutter 3.44.8 كاملاً (1.5GB) إلى `/home/user/sdk/flutter`
   — **داخل مساحة العمل الدائمة لا `/tmp`** (حجم `/tmp` 993MB
   فيمتلئ ويفشل فك الضغط).
2. إضافة **6GB swap**، لأن الذاكرة 2GB فقط وخادم التحليل
   كان يُقتَل بـ OOM (exit code -9):

```bash
sudo fallocate -l 6G /swapfile && sudo chmod 600 /swapfile \
  && sudo mkswap /swapfile && sudo swapon /swapfile
```

> **تحذير للوكيل القادم:** لقطة مساحة العمل مقيَّدة (~128MB /
> 10,000 ملف)، ولوحظ فعلياً أن مجلد `sdk/flutter` **يُبتر** بين
> الجلسات (بقي منه 60MB من 2.8GB، وفقدت الملفات صلاحية التنفيذ).
> **تحقّق قبل الاعتماد عليه:**
> ```bash
> chmod +x /home/user/sdk/flutter/bin/flutter
> flutter --version   # إن فشل، أعد التنزيل
> ```
> وإن لزم إعادة التنزيل:
> ```bash
> curl -L -o f.tar.xz "https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.8-stable.tar.xz"
> tar -xJf f.tar.xz -C /home/user/sdk && rm f.tar.xz
> ```

---

## 3. طريقة العمل المتفق عليها مع المستخدم

هذه ليست تفضيلات شكلية، كل بند منها نشأ من احتكاك حقيقي:

| القاعدة | نصّ المستخدم |
|---|---|
| **لا خيارات، نفّذ** | «انت عم تعطيني الف خيار لازم نفذهم وهذا الشي خطأ» |
| **لا سطحية** | «ماعد تشتغل بسطحية وتشتغل بطريقة صح» |
| **الدقة قبل السرعة** | «نفذ كل الاصلاحات التي انا طلبتها مهما اخذت وقت لكن بدقة كبيرة» |
| **أكمل دون مراجعة** | «اكمل كل المراحل دون الرجوع الي وبعد ان تنتهي اعطني امر تنزيل وتشغيل» |
| **أوامر جاهزة** | يريد أوامر تُلصق مباشرة، لا شروحات مطوّلة |

**اللغة:** العربية، ولهجة سورية عامية مقبولة ومحبَّذة.

**علامة `.` من المستخدم** = «أنت متأخر برسالة، الحق». **ليست طلباً
جديداً** ولا يُبنى عليها عمل.

**تحذير التوكن يُتجاهل** — طلب المستخدم ذلك صراحةً.

---

## 4. ما أُنجز في هذه الجلسة — `a35de2f`

### 4.1 الشكوى كما وردت

> «طابق المحكمة المحافظة مع كل درجات التقاضي وانواع المحاكم
> يعني بالاستئناف طلعلي شي بيضحك طابق صديقي»

### 4.2 التشخيص — عطلان متمايزان

**العطل الأول: المحكمة كانت مُعرَّفة بمحافظتها وحدها.**

قرار سابق (الإصدار 5) جرّد أسماء المحاكم إلى أسماء محافظات، بحجّة
أن الدرجة «شأن إجرائي تديره `JudicialPhases`». النتيجة أن جواب
سؤال «أمام أي محكمة استُؤنفت الدعوى؟» صار **«حمص»** — وهذا اسم
مدينة لا محكمة. الدرجة كانت تُحشر في `sub_type` كنص حر، ومنه
استحال حساب مسار الطعن.

**العطل الثاني: قائمة الانتقال بُنيت من `enum` عرضٍ عام.**

`_showAddPhaseDialog` كانت تعرض `CasePhaseType.values` كاملة:

```dart
items: CasePhaseType.values.map(...)   // ‼️
```

و`CasePhaseType` يخلط محاكم بأطوار إجرائية:
`initial, hearing, evidence, judgment, appeal, cassation, execution, settlement`

فظهر للمحامي: **«جلسات» و«إثبات» و«حكم»** كأنها محاكم يُنتقل
إليها، و**«صلح» خياراً بعد «استئناف»** — انحدار لا وجود له في
التقاضي. **هذا هو «الشي اللي بيضحك».**

### 4.3 الإصلاح

**أ. `lib/core/constants/court_catalog.dart` (جديد، 570 سطراً)**

فهرس **28 نوع محكمة**، لكل واحد: درجته، أنواع الدعاوى التي
ينظرها، أين ينعقد، هل له غرف مرقّمة، وإلى أين يُطعن بقراره.

```dart
enum LitigationDegree {
  preTrial, first, second, cassation, execution, litigationAgainstJudges
}
```

المرجع البنيوي: قانون السلطة القضائية وقانون أصول المحاكمات:
- الدرجة الأولى: الصلح، البداية، الشرعية، الأحداث، الإدارية.
- الدرجة الثانية: الاستئناف بغرفه، ومحكمة الجنايات.
- النقض: محكمة واحدة **مقرها دمشق**، دوائرها مدنية وجزائية
  وأحوال شخصية، وليست درجة تقاضٍ بل محكمة قانون.
- القضاء الإداري: المحكمة الإدارية ← محكمة القضاء الإداري ←
  المحكمة الإدارية العليا. **لا يوجد «نقض إداري».**

الدالة الحاسمة:

```dart
static List<CourtKind> nextStagesFrom(String? currentKindId)
```

المسار مُعرَّف **صعوداً** في `appealsTo`، ولا يُشتق من ترتيب
قائمة عرض. هذا ما يمنع ظهور «صلح» بعد «استئناف».

**ب. القاعدة: `schemaVersion` 5 → 6**

أعمدة جديدة على `courts` و`cases` و`case_phases`:
- `court_kind` — معرّف ثابت من الفهرس (مثل `appeal_civil`).
- `chamber_number` — رقم الغرفة.

الترحيل `_migrateToV6` يضيف الأعمدة، ثم:
- `ensureCourtKindRows()` — يحقن سجلاً لكل (نوع × محافظة) غير
  موجود. **قابل لإعادة التنفيذ بلا تكرار** (اختبار يحرس ذلك).
- `backfillCourtKinds()` — يستنتج النوع للسجلات القديمة من
  النص الحر عبر `CourtCatalog.inferKindFromText`.

> **قرار تصميمي مقصود:** الاستنتاج **يُرجع `null` عند الالتباس**
> ولا يخمّن. الفجوة الصريحة أسلم من درجة مخترعة يُبنى عليها مسار
> طعن خاطئ. النقض يُفحص **قبل** البداية لأن «نقض مدني» يحوي
> «مدني» فتلتقطه مطابقة أعم.

**ج. `court_selector.dart` (جديد، 352 سطراً)**

اختيار مركّب: **درجة ← نوع ← محافظة ← غرفة**، محصور بما يقبله
نوع الدعوى. يعرض سطر ملخّص بالمحكمة كما ستُحفظ وتُطبع.
محاكم دمشق الحصرية تُثبَّت تلقائياً بلا قائمة من عنصر واحد.

**د. حارس مزدوج على مسار الطعن**

الشاشة لا تعرض إلا الوجهات الصحيحة (`nextStagesFrom`)، **و**
`CaseRepository.transferToNextPhase` يرفض أي انتقال خارج المسار
حتى لو وصله الطلب من مكان آخر:

```dart
throw Exception('لا يجوز الانتقال من «$from» إلى «$to».');
```

ولمّا تبلغ الدعوى النقض تُخبر صراحةً: «لا توجد درجة أعلى بعد
«محكمة النقض»» بدل عرض قائمة لا تصلح.

### 4.4 أعطال إضافية اكتُشفت أثناء العمل وأُصلحت

| # | العطل | الموضع |
|---|---|---|
| 1 | **«المحكمة: 7»** — كان يُطبع `courtId.toString()` مكان اسم المحكمة | `case_detail_screen` |
| 2 | **«محكمة عمالية» مُدرجة تحت القضاء الإداري** في خارطة الأرشيف اليدوية | `archive_intake_screen` |
| 3 | **7 محافظات فقط** في حوار المحاكم — تعذّر تسجيل محكمة في إدلب/الرقة/دير الزور/الحسكة/طرطوس | `settings_screen` |
| 4 | المرحلة الأولى للدعوى كانت تُنشأ بلا نوع محكمة فينكسر حساب الطعن منها | `case_repository` |
| 5 | `_askCustomValue` دالة ميتة (كانت ميتة قبل الجلسة) | `create_case_wizard` |

### 4.5 البند المعلّق الذي أُغلق

`dart run build_runner build --delete-conflicting-outputs` **نُفِّذ
بنجاح** لأول مرة. كان `chamber_number` يُضاف بـ SQL يدوي
(`_ensureSqlColumn`) لأن `database.g.dart` لم يكن يُعاد توليده.
الآن صار عموداً مولَّداً حقيقياً و`_addColumnIfMissing` تعمل
بـ `m.addColumn` الصحيحة.

---

## 5. التحقق — ماذا فُحص فعلاً

| الفحص | النتيجة |
|---|---|
| `flutter analyze` | **صفر أخطاء** |
| `flutter test` | **187 ناجحاً • 12 فاشلاً • صفر انحدار** |
| `audit_members.dart` | لا `DUPLICATE_TOP_LEVEL` ولا `FAMILY_MISUSE` |
| `build_runner` | نجح |
| `flutter run -d windows` | ❌ **غير ممكن في بيئة Linux — لم يُنفَّذ** |

### الـ 12 اختباراً الفاشلة — قائمة من قبل هذه الجلسة

**أُثبت ذلك منهجياً**، لا بالادعاء: استُنسخت نسخة نظيفة من
`8ea79f4` في `/tmp/verify`، وشُغّلت الاختبارات عليها، وقورنت
قائمة الفشل بقائمة ما بعد التعديل:

```bash
comm -13 /tmp/base_fails.txt /tmp/new_fails.txt   # ← فارغ
```

سببها جميعاً: `StorageLocationService.initialize() لم تُستدعَ بعد`
— الاختبارات تفتح القاعدة دون تهيئة خدمة مسار التخزين التي
أُضيفت في `fde5eb7`. **لا علاقة لها بالمحاكم.**

> اختبار واحد فشل بسببي فعلاً وأُصلح: `poa_structure_test` كان
> يفحص **نص الملف** بحثاً عن سلسلة حرفية `'نقض إداري'`. نُقلت
> القائمة إلى الفهرس فسقط الفحص النصي. أُعيدت صياغته ليفحص
> **السلوك** عبر `CourtCatalog` بدل قراءة الملف.

---

## 6. المشاكل القائمة — بصراحة

### 6.1 لم يُجرَّب على ويندوز
أخطر بند. البناء الفعلي هو الحكم النهائي وفق الدستور، ولا يمكن
تنفيذه من بيئة Linux. **الأولوية الأولى: أن يشغّله المستخدم.**

### 6.2 اثنا عشر اختباراً فاشلاً (مُوَرَّثة)
تحتاج تهيئة `StorageLocationService` في `setUp`. لم تُلمس في هذه
الجلسة لأنها خارج نطاق الطلب، وتوسيع النطاق دون إذن مخالف
لطريقة العمل المتفق عليها. **مرشّح جيد للمهمة القادمة.**

### 6.3 الترحيل يستنتج ولا يجزم
الدعاوى القديمة التي نصّها غامض ستبقى بلا `court_kind`. هذا
مقصود. **على المستخدم مراجعة الدعاوى القديمة** وتصحيح المحكمة
يدوياً من الويزارد عند اللزوم.

### 6.4 السجلات القديمة في `courts`
الأربعة عشر صفاً القديمة (اسم محافظة بلا نوع) **تُترك عمداً** كي
لا تنكسر الدعاوى المرتبطة بها عبر `court_id`. لا تظهر في قوائم
الاختيار الجديدة لأنها تُفلتر بـ `court_kind IS NOT NULL`.

### 6.5 جدول `courts` كبر
من 14 صفاً إلى ~330. مقصود ومختبَر، لكن **إن شكا المستخدم من
بطء** فالحل فهرس:
```sql
CREATE INDEX idx_courts_kind ON courts(court_kind, name);
```

### 6.6 هشاشة بيئة الوكيل
لوحظ في هذه الجلسة **تراجع مساحة العمل**: اختفت تعديلات محلية
وبُتر مجلد SDK. **الدرس: ارفع إلى GitHub مبكراً وكثيراً**؛ هو
مصدر الحقيقة الوحيد الموثوق. (هذا ما أنقذ عمل هذه الجلسة.)

---

## 7. الخطوات التالية — مرتّبة

1. **[حرج]** المستخدم يشغّل الأوامر في §1 ويبلّغ بالنتيجة.
2. **[حرج]** تجربة وظيفية: إنشاء دعوى مدنية أمام البداية في
   محافظة ما، ثم نقلها للاستئناف، والتأكد أن:
   - قائمة الوجهات لا تحوي «جلسات» ولا «إثبات» ولا «حكم».
   - لا يظهر «صلح» بعد «استئناف».
   - اسم المحكمة يظهر كاملاً: «محكمة الاستئناف المدنية في حمص —
     الغرفة 2» لا «حمص» ولا رقماً.
3. **[مهم]** إصلاح الاختبارات الاثني عشر (تهيئة
   `StorageLocationService`).
4. **[معلّق من جلسة سابقة]** ميزات `fde5eb7` لم تُجرَّب بعد:
   تصفير البيانات، تغيير مسار التخزين، تلوين أقسام القائمة
   الجانبية.
5. **[اقتُرح ولم يُقرّ]** شريط بحث فوري أعلى شاشتَي الدعاوى
   والملفات. قال المستخدم «شفتها وحلوة الفكرة» لكنه انصرف لغيرها.

---

## 8. خرائط سريعة للملفات

### أُنشئت في هذه الجلسة
```
lib/core/constants/court_catalog.dart          ← الفهرس القضائي (المرجع)
lib/presentation/widgets/common/court_selector.dart  ← الاختيار المركّب
test/court_catalog_test.dart                   ← 30 اختباراً
test/court_transfer_flow_test.dart             ←  5 اختبارات
```

### عُدّلت جوهرياً
```
lib/data/database/schema.dart          ← court_kind + chamber_number
lib/data/database/database.dart        ← v6 + الترحيل + الحقن
lib/data/database/daos/lookup_dao.dart ← watchCourtsOfKind / findCourt
lib/data/repositories/case_repository.dart      ← حارس مسار الطعن
lib/presentation/screens/cases/case_detail_screen.dart   ← حوار النقل
lib/presentation/screens/cases/create_case_wizard.dart   ← الحقل المركّب
lib/presentation/screens/archive_intake/...     ← توحيد الخارطة
lib/presentation/screens/settings/...           ← 14 محافظة + نوع المحكمة
CONSTITUTION.md                                 ← 4 محظورات جديدة
```

### معرفة تقنية متراكمة (لا تُكتشف مرتين)
- `activeCourtsProvider` هو `StreamProvider.family<List<Court>, String?>`
  → يُستدعى `activeCourtsProvider(null)`.
- المزوّد الصحيح لدعوى واحدة هو **`caseDetailFromRepoProvider`**،
  ولا وجود لـ `caseByIdProvider`.
- `CaseType` معرّف في `presentation/screens/cases/case_models.dart`
  **لا** في `core/enums/app_enums.dart`.
- المرفقات مشفّرة AES بلاحقة `.enc` → تُفتح عبر
  `AttachmentService`. **القوالب لا تُشفَّر** وتُفتح مباشرة.
- `DropdownButtonFormField.value` مهجورة في 3.44 → `initialValue`.
- سلسلة التعارض: `pdf 3.13 → xml ^7 → image ≥4.9 → archive ^4`
  محلولة عبر `dependency_overrides`.

---

## 9. سجل الـ commits

```
a35de2f  مطابقة المحكمة مع درجة التقاضي ونوعها     ← هذه الجلسة
8ea79f4  الدستور: بوابة رفع إلزامية
072dd1b  إصلاح ثلاثة أخطاء بناء + أداة تمنع تكرارها
fde5eb7  مسار تخزين مخصص + تصفير البيانات + تلوين
1826028  إصلاح حفظ الدعوى + تصنيف تلقائي + خصوم ومرفقات
ed12358  إصلاح خطأي بناء: Ref/WidgetRef و chamber_number
e3444c8  تدقيق الكتابات: فتح نسخ العقود المشفّرة
e727365  تدقيق AST: ستة أزرار ميتة أُصلحت
50b538c  جولة تفتيش ثانية: موضعان من النجاح الكاذب
4844c76  تفتيش «النجاح الكاذب»: ستة مواضع
3c508d2  تنفيذ الملاحظات الأربع المفتوحة
ea3e2eb  اختبارات وتوثيق
8aed1ed  إكمال البندين 1.3.4 و5.3.1
419d13f  المرحلتان 5 و6
e403e5a  المراحل 1-4
5c34af7  المرحلة 0: الترحيل الصحيح
```

---

## 10. تذكير بالدستور

`CONSTITUTION.md` **مُلزِم**. قبل أي رفع:

| # | الفحص | الأمر |
|---|---|---|
| 1 | التحليل | `flutter analyze` |
| 2 | الأعضاء | `dart run tool/audit/audit_members.dart` |
| 3 | الاختبارات | `flutter test` |
| 4 | البناء | `flutter run -d windows` |

**إن تعذّر فحص، يُصرَّح بذلك نصاً** — لا يُقال «تم التحقق» عن فحص
لم يُنفَّذ. أُضيفت في هذه الجلسة أربعة محظورات: تعريف المحكمة
بمحافظتها وحدها، اشتقاق خيارات الانتقال من `enum` عرض، تكرار
قائمة مرجعية قانونية في شاشة، وحصر المحافظات في بعضها.
