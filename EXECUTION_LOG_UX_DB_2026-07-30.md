# سجل تنفيذ — إصلاحات الترحيل وتجربة الإدخال
**التاريخ:** 2026-07-30 · **الحالة:** ✅ كل المراحل منفّذة

مرجع الخطة: `EXECUTION_PLAN_UX_DB_2026-07-30.md`

---

## المرحلة 0 — الترحيل الصحيح ✅

| البند | التنفيذ |
|---|---|
| رفع الإصدار | `schemaVersion` 3 ← **5** |
| ترحيل v4 | 14 `addColumn` صريحة (13 للوكالات + `office_file_id`) |
| ترحيل v5 | `chamber_number` + تطبيع أسماء المحاكم |
| حماية | `_addColumnIfMissing` تفحص `PRAGMA table_info` أولاً |
| شبكة أمان | `ensurePoaColumns` / `ensureUpgradeTableColumns` باقية |
| اختبار | `test/schema_migration_test.dart` |
| دستور | قاعدة إلزامية في المحظورات التقنية |

**نقطة دقيقة:** `addColumn` وحدها تفشل على قاعدة سبق أن أضافت شبكة
الأمان أعمدتها، لأن `ALTER TABLE ADD COLUMN` يرفض عموداً مكرراً.
لذلك يسبق كل إضافة فحصُ وجود.

---

## المرحلة 1 — البحث في الحقول المرجعية ✅

`lib/presentation/widgets/common/searchable_picker.dart`

- تطبيع عربي: الهمزات (`أإآ`→`ا`)، التاء المربوطة (`ة`→`ه`)،
  الألف المقصورة (`ى`→`ي`)، إزالة التشكيل.
  فيطابق «احمد» = «أحمد»، و«حماه» = «حماة».
- بحث في حقول إضافية عبر `searchTermsOf` (الهاتف، الرقم الوطني).
- إنشاء عنصر جديد داخل نافذة البحث عبر `onCreateNew`.
- اختبار: `test/searchable_picker_test.dart`

طُبّق على: الشكل القانوني، الشريك، المدير، المحكمة.

---

## المرحلة 2 — بنية المحاكم ✅

| قبل | بعد |
|---|---|
| «محكمة البداية المدنية الأولى بدمشق» | «دمشق» + غرفة رقم |
| «محكمة حما» | «حماة» |
| «محكمة الاستئناف» / «محكمة النقض» | حُذفت — الدرجة في `JudicialPhases` |

- 14 محافظة في `AppConstants.syrianGovernorates`.
- `chamber_number`: 1..16 مع إدخال يدوي لما فوقها.
- `normalizeCourtNames()` تستخرج المحافظة من التسميات القديمة،
  وتقدّم «ريف دمشق» على «دمشق» حتى لا تُبتلع.
- **تُنفَّذ ضمن الترحيل فقط**، لا في `beforeOpen`: تشغيلها كل إقلاع
  كان سيعيد كتابة أسماء أدخلها المستخدم عمداً.
- اختبار: `test/courts_governorate_migration_test.dart`

---

## المرحلة 3 — رقم الدعوى التلقائي ✅

**اكتشاف أثناء التنفيذ:** `createCase` كان **أصلاً** يستبدل `'TMP'`
برقم ملف المكتب (`officeFile.fileNumber`) بصيغة `دعوى/2026/0001`.
فالرقم الصحيح كان يُولَّد لكنه غير معروض، والمستخدم يُطلب منه إدخاله.

| البند | التنفيذ |
|---|---|
| المعاينة | `peekNextFileNumber` تقرأ العدّاد دون استهلاكه |
| الحقل | للقراءة فقط مع مؤشر تحميل |
| الشرح | «تسلسل الدعوى ضمن دعاوى المكتب… حالياً N جارية • M منتهية» |
| العدّادات | `activeCasesCountProvider` / `closedCasesCountProvider` |

العدّادان مشتقان من تدفق الدعاوى، فإغلاق أي دعوى ينعكس تلقائياً.
الرقم يبقى ثابتاً بعد الانتهاء (قرار معتمد) حفاظاً على المراسلات.

---

## المرحلة 4 — دمج عنوان الدعوى وموضوعها ✅

حقل واحد: **«موضوع الدعوى»**. العنوان يُشتق منه تلقائياً حفاظاً على
توافق السجلات القديمة التي تقرأ `title`.

---

## المرحلة 5 — المرفقات ✅

**اكتشاف حاسم:** المرفقات تُخزَّن **مشفّرة AES** بلاحقة `.enc`.
تسليم مسارها لـ `OpenFilex` مباشرة لا يفتح شيئاً — يجب فكّ التشفير
إلى ملف مؤقت أولاً (`getFileFromRelativePath` تقوم بذلك).

`lib/data/services/attachment_service.dart`:

| الوظيفة | التفصيل |
|---|---|
| `pickAttachment` | فلترة + تحقق بعد الاختيار + حد 50 ميجابايت |
| `openStoredAttachment` | فكّ تشفير ثم فتح |
| `revealInFolder` | إظهار في المستكشف (ويندوز/ماك/لينكس) |
| `isPreviewable` | هل تُعرض داخلياً؟ |

### الصيغ المعتمدة
`pdf` · `doc` `docx` `rtf` · `jpg` `jpeg` `png` `webp` `heic` ·
`tif` `tiff` · `txt` · `xls` `xlsx` `csv`

ما عداها يُرفض برسالة تذكر المسموح.

### ⚠️ عطل جوهري أُصلح
أزرار «فتح» و«تنزيل» في `document_viewer.dart` كانت تعرض رسالة
«تم فتح الملف» **دون فتح أي شيء**. صارت تفتح فعلياً وتُظهر في المجلد،
مع رسائل دقيقة عند الفشل (لا تطبيق مناسب / ملف مفقود / صلاحية).

---

## المرحلة 6 — ويزارد الشركة ✅

| # | العطل | الإصلاح |
|---|---|---|
| 1 | نص أبيض على خلفية فاتحة | لون صريح حسب حالة الاختيار |
| 2 | الشكل القانوني لا يتبع الاختيار | `SearchablePicker` مع ربط |
| 3 | «عدد الشركاء» | «الشركاء» + مجموع النسب |
| 4 | الشريك من السجل فقط | نموذج إضافة: اسم ثلاثي، هاتف، بريد، عنوان |
| 5 | شريك بلا حصة/نسبة | تحقق إلزامي — `tryParse ?? 0` كان يبتلع الفراغ |
| 6 | لا خيار لمدير جديد | نفس نموذج الإضافة |
| 7 | مدير فارغ يختفي | تحقق من الشخص والصلاحيات |
| 8 | «اسم موجود مسبقاً» خاطئ | مطابقة بعد التطبيع + ذكر الشركة |
| 9 | تعذّر الرجوع | `onStepTapped` |
| 10 | عرض `ID: 12` | عرض الاسم |
| 11 | — | منع تكرار الشريك وتجاوز 100% |

---

## التحقق المنفَّذ

| الفحص | النتيجة |
|---|---|
| تحليل نحوي (160 ملفاً) | ✅ نظيف |
| مطابقة أسماء الأعمدة لـ `database.g.dart` | ✅ 54 عموداً |
| حقول `PersonsCompanion` / `CompanyPartnersCompanion` | ✅ مؤكدة |
| منطق الترحيل بمحاكاة SQLite | ✅ أُعيد إنتاج الخطأ ثم إصلاحه |
| استخراج المحافظات (16 حالة) | ✅ كلها صحيحة |
| التطبيع العربي (8 حالات) | ✅ كلها صحيحة |

**لم يُنفَّذ من طرفي:** `flutter analyze` و`flutter test` الكاملان —
البيئة لا تسمح بتحميل Flutter SDK كاملاً. الفحص جرى بمحلل Dart
الرسمي ومحاكاة SQLite.

---

## اختبارات مضافة
- `test/schema_migration_test.dart`
- `test/courts_governorate_migration_test.dart`
- `test/searchable_picker_test.dart`

---

## ملاحظات مفتوحة

1. **`AutomationService` غير مستدعى** من أي مكان — ملف قائم بلا مستهلك.
2. **التقرير الأسبوعي** على `pw.Page` واحدة، ينقطع إذا كثرت المواعيد.
3. **`backup_service.dart:119`** — `file.content as List<int>` بعد
   ترقية `archive` إلى 4.x قد يكتب ملفات فارغة بصمت عند الاستعادة.
4. **أنواع الشركات** — ويزارد واحد لكل الأنواع؛ لم تُفحص ملاءمته
   لكل نوع على حدة.
